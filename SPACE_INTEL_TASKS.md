# 商业航天全量情报站任务进度

本文件用于跟踪开发任务状态。发现的 bug、线上问题和回归验证记录放在 `SPACE_INTEL_ISSUES.md`。

## Status Legend

- `TODO`：未开始。
- `IN_PROGRESS`：正在开发。
- `BLOCKED`：被外部条件阻塞。
- `DONE`：已完成并通过必要验证。

## Current Development Plan

更新时间：2026-06-02

当前重点：v1 已具备 React/Vite/TypeScript 前端、Cloudflare Pages Functions API、D1 schema、采集管线、人工精选、首页排序、主要业务页面、Git-backed Pages 部署、scheduled Worker、R2、production ingestion、catalog sync、实体关联、launch cache、Hy-MT 翻译链路和 D1 数据保留策略。当前开发重心从“大功能补齐”转为治理文档清晰化、线上采集质量观察、回归问题闭环和后续增强筛选。

当前任务：

- IN_PROGRESS：国内来源采集诊断能力改进正在闭环。已针对 `SI-ISSUE-353` 增加公开 `/api/health` 的脱敏错误分类和耗时字段，并新增受保护的单源采集重跑入口与受保护的最近失败采集日志入口，避免只能看到 `hasError: true` 而无法定位超时、HTTP、解析或 D1 问题；目标测试、完整回归、配置检查、类型检查、lint、构建和 diff check 已通过，仍需部署后生产复查。
- DONE：站点品牌、官方栏目和公开来源扩展已完成。站点名统一为“航天信息”，第 4 个主入口改为“官方”并新增 `/official`，旧 `/policy` 保持兼容；公开 API 支持 `category=official`，仅筛选官方页和采购公告中带政策/采购标签的记录；来源治理已改写为“扩大公开来源、分级管理风险”，新增 CNSA 政策公告、中国政府采购网地方公告、全国公共资源交易平台航天公告、卫星通信观察、未来天玑动态和中关村商业航天联盟等公开来源；采购公告采集不再用采集时间兜底发布日期。目标测试、配置检查、类型检查、lint、完整回归、构建、布局验证、本地 `/official` mobile 检查、diff check 和新增行敏感信息扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-352`。
- DONE：官方页新闻时间和链接质量修复已完成。生产复查确认首页/资讯页顶部存在用采集时间冒充发布时间的官方页记录，并包含公司主页、采购平台首页、栏目页等非新闻链接；本轮已要求官方页候选必须带真实来源日期，过滤首页/栏目/index/default URL，解析蓝箭等公司页尾部中文日期，并屏蔽历史 crawl-time 官方页脏数据。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和新增行敏感信息扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-351`。
- DONE：最新资讯来源质量修复已完成。生产复查确认最新页被 `国家航天局新闻` 栏目/导航页污染，来源状态也暴露历史启用但已不在配置中的来源；本轮已收紧 CNSA 采集 URL 规则、公共查询屏蔽历史导航页、来源 API 仅展示配置中仍启用的来源，并在直接国内源不足的前提下启用少量带访问提示的 Google News 中文备份聚合源。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和新增行敏感信息扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-350`。
- DONE：HTML 短链接过滤已补齐零宽字符标题变体。短标题过滤此前会移除普通空白，但 `查\u200b看全部`、`详\u200b情` 这类零宽字符混淆导航文本仍可能绕过通用入口过滤；现在短标题归一化也会移除常见零宽格式字符。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-349`。
- DONE：HTML href 过滤已补齐混淆联系方式协议识别。普通 `mailto:` 会被 HTTP(S) URL 规范化拒绝，但 `mail to:` 或 `te\u200bl:` 这类空白/零宽字符混淆可能在带 base URL 解析时变成相对路径；现在紧凑非网页协议检查会拒绝混淆后的联系方式 href。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-348`。
- DONE：HTML href 过滤已补齐零宽字符混淆伪协议识别。`SI-ISSUE-346` 已处理 Unicode 空白，但 `java\u200bscript:alert(1)` 这类零宽字符混淆仍可能绕过紧凑判断；现在紧凑伪协议检查会移除常见零宽格式字符。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-347`。
- DONE：HTML href 过滤已补齐 Unicode 空白混淆伪协议识别。`SI-ISSUE-345` 已处理 ASCII 空白/控制字符混淆，但原始 href 中的 NBSP 等 Unicode 空白仍可能让 `java\u00a0script:` 这类伪协议绕过紧凑判断；现在紧凑伪协议检查会移除 JavaScript trim 语义下的空白字符。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-346`。
- DONE：HTML href 过滤已补齐空白混淆伪协议识别。共享占位链接判断此前会拒绝 `javascript:`，但 `java script:alert(1)`、`java\\tscript:alert(1)` 或 `data :text/html,hi` 这类写法可能在带 base URL 解析时变成相对路径；现在会先做紧凑伪协议判断再进入 HTTP(S) 归一化。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-345`。
- DONE：HTML 占位 href 过滤已补齐脚本占位变体。共享占位链接判断此前会拒绝 `void(0)` 和 `javascript:`，但 `void 0`、`return false` 这类旧模板占位可能被当成相对 URL 进入候选；现在这些非导航脚本占位会在 HTTP(S) 归一化前被拒绝。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-344`。
- DONE：HTML 占位 href 过滤已补齐多层花括号归一化。共享占位链接判断此前只剥一层 `{}`，`{{javascript:;}}` 或 `{{#}}` 这类旧模板占位可能绕过伪链接判断并被当作相对 URL；现在会重复剥离成对外层花括号后再判断占位和伪协议。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-343`。
- DONE：HTML 短链接过滤已补齐嵌套括号归一化。共享短标题过滤此前只剥一层包装，`【[详情]】` 或 `（【2026-05-28】）` 这类嵌套详情/日期入口可能绕过过滤进入采集候选；现在短标题检查会剥多层常见包装后再判断通用导航和日期-only 标题。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-342`。
- DONE：HTML 数字实体解码已补齐无分号兼容。共享 HTML 解码此前能处理 `&#x5e74;` / `&#24180;`，但旧页面如果输出 `&#x5e74` / `&#24180`，标题和日期上下文会残留实体文本并影响发布日期识别；现在十进制和十六进制数字实体支持安全边界下的无分号形式，非法或控制字符码点仍保留原文。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-341`。
- DONE：中文官网日期识别已补齐分隔符空格兼容。共享日期解析此前能识别 `2026年5月30日`，但会漏掉 `2026 年 5 月 30 日` 这类政府页常见写法；现在邻近日期、日期-only 链接过滤、发布日期提取和标题前缀清理都兼容 year-first 日期分隔符空格，非法日期校验保持不变。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-340`。
- DONE：标题前导日期清理已补齐时间戳残留处理。共享标题清理此前能移除 `2026-05-30`，但遇到 `2026-05-30T08:00:00Z 商业航天公告` 或 `2026-05-30 08:00 商业航天公告` 这类标题时，公开标题可能留下时间片段；现在有效日期后的 ISO/时钟时间片段会一并清理，非法日期前缀仍保留原文。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-339`。
- DONE：独立 HTML 卡片邻近日期查找已补齐真实日期校验。共享独立锚点抽取此前会按日期形状选最近上下文，如果最近的是 `2026-13-40` 这类非法日期，可能压过同一卡片里稍远但有效的发布日期；现在邻近日期候选必须能解析为真实日期才会进入上下文。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-338`。
- DONE：独立 HTML 卡片邻近日期查找已补齐跨卡片边界保护。共享独立锚点抽取此前会在锚点前后 220 字符内找日期，紧凑卡片列表中上一条可能误借下一条卡片日期；现在跨相邻块级卡片容器的日期不会被当作同一卡片上下文，同卡片附近日期仍保留。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-337`。
- DONE：标题前导日期清理已补齐真实日期校验。共享标题清理此前只按日期形状移除前缀，`2026-13-40 商业航天公告` 这类非法日期也会被删掉，尽管发布日期提取不会使用它；现在只有可解析为真实日期的前缀才会从公开标题移除。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-336`。
- DONE：HTML 无分号实体解码边界已收紧。`SI-ISSUE-334` 兼容 `&amp` 和 `&nbsp` 后，旧正则也会匹配普通文本前缀，例如 `&amplifier` 或 `&nbspword`，可能误改标题或上下文。现在无分号实体解码只在明确分隔边界生效，普通前缀会保留原文；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-335`。
- DONE：HTML 常见无分号实体解码已补齐。共享 HTML 解码此前只处理 `&amp;`、`&nbsp;` 这类标准写法，旧站如果输出 `&amp` 或 `&nbsp`，可能让 href 查询串和可见标题保留噪声。现在共享解码兼容这两类常见无分号实体；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-334`。
- DONE：HTML 短链接括号归一化已补齐。共享 HTML 抽取已经能过滤 `[查看全部]`、`【详情】` 这类短入口，但 `(查看全部)`、`（详情）` 这类圆括号变体此前仍可能作为候选进入采集。现在短标题过滤会统一剥离 ASCII 和全角圆括号，圆括号包裹的详情、查看全部和日期-only 链接都会被一致过滤；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-333`。
- DONE：HTML 锚点标题属性读取范围已收紧。共享 HTML 抽取此前会在整个 `<a>...</a>` 片段里查找 `title`，如果锚点内部图片或图标带有 `title="缩略图"`，可能覆盖真实可见文章标题。现在标题属性只从 opening `<a ...>` 标签读取，内部图片或图标标题不再覆盖正文标题；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-332`。
- DONE：HTML 锚点属性解析已收紧。共享 HTML 抽取此前用单词边界识别 `href` 和 `title`，可能把 `data-href` / `data-title` 误当成真实链接或真实标题；如果埋点属性排在真实属性前面，会把跟踪 URL 或低质量标签带进采集候选。现在锚点抽取只接受空白分隔的真实 `href` / `title` 属性，目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-331`。
- DONE：HTML 卡片 `title` 属性旧式写法兼容已补齐。HTML 链接 `href` 已支持 `href = "..."` 这类旧站属性格式，但卡片标题优先读取仍只识别 `title="..."`，遇到 `title = "真实标题"` 时会退回整段锚点正文，可能把日期和摘要混进公开标题。现在共享锚点标题解析支持等号周围空白和无引号属性值，并继续优先使用 `title` 属性替代噪声卡片正文；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-330`。
- DONE：采购页前导日期标题清理已补齐。采购页采集会从列表文本提取日期写入 `publishedAt`，但 `【2026年5月30日】某卫星遥感数据采购项目中标公告` 这类标题此前仍会把日期留在公开标题和摘要中。现在采购记录会复用共享前导日期清理，公开标题和摘要去掉重复日期，并仅在标题被清理时保留原始标题；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-329`。
- DONE：官方页括号日期标题清理已补齐。官方页采集已经会用标题开头日期生成 `publishedAt`，也会清理裸日期前缀，但 `【2026年5月30日】商业航天政策公告发布` 这类常见括号日期仍会留在公开标题和摘要里。现在官方页公开标题会清理带方括号、书名号式括号和圆括号的前导日期，同时保留原始标题；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-328`。
- DONE：HTML 列表 legacy href 属性兼容已补齐。官方页和旧站页面可能输出 `href=./article.html` 或 `href = "./article.html"` 这类有效锚点，但抽取正则此前只识别无空格的引号形式，可能在相关性过滤前漏掉真实列表项。现在列表块和独立卡片抽取共用同一个 anchor matcher，支持双引号、单引号、无引号和等号周围空白；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-327`。
- DONE：HTML 列表 href 实体解码边界已修复。官方页和采购页抽取此前把原始 `href` 属性直接交给 URL 归一化，`?keyword=space&amp;page=1` 这类常见 HTML 查询串可能保留字面 `amp;`，`javascript&#58;alert(1)` 这类编码伪协议也会在非网页链接检查前被当成相对路径。现在 href 会先解码 HTML 实体，再执行占位链接和 HTTP(S) 归一化；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-326`。
- DONE：前端外链组件的 credentialed URL 回归保护已完成。文章卡片、文章详情、发射详情、公司详情和专题详情都依赖 `safeExternalUrl` 渲染站外链接，但组件测试此前主要覆盖 `javascript:` / `data:` scheme，没有直接防止后续页面绕过共享 helper 后渲染 `https://user:pass@example.com/...` 这类公开链接。现在这些外链入口都补充了 credentialed URL 不渲染断言，同时保留原有 unsafe scheme 覆盖；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-325`。
- DONE：公司和专题公开 serializer 的 credentialed URL 回归保护已完成。公司官网/Logo 与专题精选资料公开序列化已复用共享 URL helper，但边界测试此前只覆盖 `javascript:`、`data:`、空白和普通有效 URL；共享 helper 收紧后，这些公开 API 契约仍缺少直接防止后续绕过 `https://user:pass@example.com/...` 的测试。现在已补充公司 website/logoUrl 和专题精选 itemUrl 的 credentialed URL 断言；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-324`。
- DONE：公开 API serializer 的 credentialed URL 回归保护已完成。文章和发射公开序列化已复用共享 URL helper，但边界测试此前只覆盖 `javascript:`、`data:` 和空白 URL；共享 helper 收紧后，公开 API 契约缺少直接防止后续绕过 `https://user:pass@example.com/...` 的测试。现在已补充文章 URL 和发射来源 URL 的 credentialed URL 断言；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-323`。
- DONE：配置 URL 校验文案与公开 URL 规则对齐已完成。共享 URL helper 已拒绝 credentialed URLs 后，source、catalog 和 curation schema 仍提示 URL “must use http or https”，对 `https://user:pass@example.com/feed.xml` 这类协议合法但不适合公开配置的值不够准确。现在错误文案已调整为 public http/https URL，并补充 source/curation credentialed URL 回归断言；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过，扫描仅命中预期的 `url.password` URL 属性防护代码。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-322`。
- DONE：共享 HTTP URL 凭据边界收紧已完成。`normalizeHttpUrl` 此前只检查 `http/https` 协议，`https://user:pass@example.com/article` 这类带用户名或密码段的 URL 可能通过公开外链、公司/精选/来源配置、采集器 URL 归一化和 API 序列化边界。现在统一 URL helper 会拒绝 credentialed URLs；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过，扫描仅命中预期的 `url.password` URL 属性防护代码。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-321`。
- DONE：详情路由动态段边界空白清理已完成。共享 route helper 此前直接编码动态值，历史 API row 或后续 serializer 如果传入 ` rocket-lab ` 这类 padded 标识，前台会生成 `/companies/%20rocket-lab%20` 这类脏 URL；现在编码前会 trim 边界空白，同时保留内部空格、斜杠和查询字符编码。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-320`。
- DONE：公司 taxonomy 列表去重和公开标签空白兼容已完成。company `sector` 配置此前会保留 `Launch, launch` 这类重复有效值，公开 taxonomy label helper 也只 trim 不压缩内部空白，历史 DB 或手工同步数据中的 `Satellite   internet` 可能显示为 `赛道待确认` 或重复标签。现在共享 taxonomy helper 会压缩内部空白，公开标签和配置解析都会按大小写不敏感键去重 sector 列表；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-319`。
- DONE：catalog taxonomy 字段内部空白兼容已完成。公司 `country` 和逗号分隔的 `sector` 此前只做 trim 后进入枚举校验；后续 catalog 配置如果写入 `United   States` 或 `Satellite   internet`，会被误拒，或者不能规范写入 catalog。现在 country 和 sector 会在校验前压缩内部空白，sector 列表也会规范逗号分隔；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-318`。
- DONE：人工精选备注内部空白归一化已完成。curation `note` 此前只做 trim；后续精选配置如果写入连续空格或制表符，parsed records 和 config sync 仍可能保留噪声备注。现在 home、pinned 和 topic curation notes 会在配置解析阶段压缩内部空白；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-317`。
- DONE：catalog 展示文本内部空白归一化已完成。公司名称、英文名、简介、股票代码、专题名称和专题关键词此前在解析层只做 trim；后续 catalog 配置如果写入连续空格或制表符，generated config 和 catalog sync 仍可能保留噪声元数据。现在公司/专题展示文本会在配置解析阶段压缩内部空白，专题关键词去重也使用归一化后的关键词；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-316`。
- DONE：来源默认关系元数据内部空白归一化已完成。source `default_tags/default_companies` 此前会 trim 并按小写键去重，但不会压缩内部空白；后续新增来源如果写入 `Rocket   Lab`，可能把噪声公司名带进采集输出，或在默认关系校验中无法匹配 catalog 的 `Rocket Lab`。现在默认关系列表和校验引用键都会压缩内部空白；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-315`。
- DONE：来源展示和治理文本空白归一化已完成。source `name/purpose/expected_content/risk_notes/access_note/public_badge` 此前在解析层只做 trim，后续新增来源如果写入连续空格或制表符，generated config 和 D1 sources catalog sync 仍可能保留噪声元数据。现在这些来源文本会在配置解析阶段压缩内部空白；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-314`。
- DONE：来源过滤词空白和大小写归一化已完成。source `include_terms/exclude_terms` 此前只做 trim 并按精确字符串去重；后续新增来源如果写入 `Commercial   Space` 和 `commercial space`，会保留重复有效过滤词，并让多空格短语降低正文匹配命中。现在来源过滤词会压缩内部空白并按大小写不敏感键去重；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-313`。
- DONE：HTML 列表抽取转义标签清理已完成。官方页和采购页共享的 HTML 列表 helper 此前只会在 decode 前删除真实标签；上游页面如果把标签转义为 `&lt;span&gt;...&lt;/span&gt;`，decode 后仍可能把可见标签文本带进标题、上下文、摘要或相关性信号。现在列表文本抽取会在 decode 前后清理标签，并保留真实 `span` 作为日期/标题分隔；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-312`。
- DONE：采集器转义 HTML 标签清理已完成。共享采集文本 helper 此前会先删真实标签再 decode 实体，`collectorDisplayText` 也只 decode；上游如果返回 `&lt;b&gt;Rocket&lt;/b&gt;` 这类转义标签，可能在 decode 后重新留下可见 HTML 标签。现在共享采集文本清理会在 decode 前后都清理标签，Google News 标题/发布方/原题也复用同一 helper；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-311`。
- DONE：SNAPI 文章文本归一化已完成。Spaceflight News API 采集器此前只对标题、来源/发布方和摘要做 trim；上游如果返回重复空白或摘要 HTML，会让噪声文本先进入 normalized article record，再依赖公开层兜底清理。现在 SNAPI 标题和来源/发布方复用采集显示归一化，摘要复用 HTML stripping 和空白归一化；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-310`。
- DONE：发射缓存文本元数据归一化已完成。Launch Library mission、rocket、provider、site 和 status 此前直接从上游 payload 写入缓存；公开层虽有显示归一化，但脏空白仍会进入 `launches` 并影响搜索/筛选质量。现在采集器会在边界处清理发射文本元数据并保留既有兜底；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-309`。
- DONE：发射窗口 ISO 规范化已完成。Launch Library `net` 此前会直接进入 `windowStart`；后续如果上游返回可解析但非 ISO 的日期文本，或无效占位字符串，会影响 `launches.window_start` 的文本排序、即将发射筛选和保留清理。现在有效窗口会转成 ISO，空白或非法窗口转为 `null`；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-308`。
- DONE：采集发布日期 ISO 规范化已完成。`collectorPublishedAt` 此前只验证日期可解析，但会把 RSS 常见的 RFC 日期文本原样返回；这类值进入 `articles.published_at` 后会削弱按文本排序、保留清理和近期统计的稳定性。现在有效上游日期会统一转成 ISO 字符串；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-307`。
- DONE：来源去重策略执行链路已完成。`dedupe_strategy` 已有配置字段，但采集入口此前生成 dedupe hash 时没有传入该字段，只靠 `google-news-*` key 前缀推断 canonical 去重；后续 RSSHub 或其他来源即使配置为 `canonical_url_title` 也不会按配置生效。现在 `collectSource` 会把来源配置策略传给 dedupe key/hash 生成；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-306`。
- DONE：来源去重策略枚举收紧已完成。source `dedupe_strategy` 此前只要求非空字符串；后续来源配置如果拼成 `url-title-source` 这类不受支持的值，会通过解析并同步进 D1 sources 元数据。现在解析层已限制为当前支持的 `url_title_source`、`canonical_url_title` 和 `external_id`，并补充类型约束；目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-305`。
- DONE：来源 URL 规范化已完成。source `url` 此前会用 `normalizeHttpUrl` 校验协议，但记录仍保留校验前字符串；后续来源配置如果写入 `HTTPS://API.SPACEFLIGHTNEWSAPI.NET/v4/articles/` 这类非 canonical URL，会把噪声 URL 带入 generated config 和采集请求。现在解析层会存储 canonical HTTP(S) URL，目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-304`。
- DONE：公司 catalog URL 规范化已完成。公司 `website/logo_url` 此前会用 `normalizeHttpUrl` 校验协议，但记录仍保留校验前字符串；后续公司配置如果写入 `https://WWW.ROCKETLABUSA.com` 这类大小写 host，会把非 canonical URL 带入 generated config 和公开公司 payload。现在解析层会存储 canonical HTTP(S) URL，目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-303`。
- DONE：人工精选 URL 规范化重复检查已完成。curation URL 此前会用 `normalizeHttpUrl` 校验协议，但记录仍保留校验前字符串；后续人工配置如果同时写入 `https://EXAMPLE.com/top` 和 `https://example.com/top`，会绕过重复目标检查并重复入库。现在解析层会存储 canonical HTTP(S) URL，目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-302`。
- DONE：专题关键词大小写变体去重已完成。实体匹配会对文章文本和 topic keywords 做大小写不敏感匹配，但 `parseTopicsConfig` 此前只按精确 trim 后字符串去重；后续新增专题如果同时写入 `reusable rocket` 和 `Reusable Rocket`，会把重复有效关键词带进 generated config 和匹配循环。现在专题关键词会按小写关键词键去重并保留首次配置写法，目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-301`。
- DONE：来源默认关系元数据大小写变体去重已完成。默认关系校验、文章入库和实体回填都已经支持大小写不敏感关系 lookup，但 `parseSourcesConfig` 此前仍按精确字符串去重 `default_tags/default_companies`；后续新增来源如果同时写入 `Rocket Lab` 和 `rocket lab`，会把重复关系元数据带到采集输出再依赖后续边界清理。现在默认标签和默认公司会按 trim/lowercase reference key 去重并保留首次配置值，目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-300`。
- DONE：来源默认关系校验大小写兼容已完成。文章入库和实体回填已经支持大小写不敏感 lookup，但 `assertValidSourceDefaultReferences` 此前仍按精确字符串校验 catalog 引用；后续新增来源如果写入 `Reusable-Rockets` 或 `rocket lab` 这类大小写变体，可能在配置检查阶段被误拒。现在校验端会 trim/lowercase 比较，并保留原始配置值用于错误提示。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和源码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-299`。
- DONE：实体回填关系 slug 大小写兼容已完成。文章入库关系 lookup 已完成大小写兼容，但 admin/daily 的 `upsertConfiguredEntityLinks` 此前仍从原始 `companySlugs/topicSlugs` 构造语句，按精确字符串去重并用 `slug = ?` 匹配；未来 matcher 或手动 enrichment 如果返回 `Rocket-Lab` / `Reusable-Rockets` 变体，可能产生冗余 no-op 或漏挂关系。现在实体回填会 trim/lowercase/drop blank/dedupe，并用 `LOWER(slug)` 匹配。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-298`。
- DONE：文章入库实体关系大小写兼容已完成。文章持久化关系写入此前只 trim 标签和公司值，并用精确 `slug/name/english_name` 匹配；历史采集或后续来源默认关系如果出现 `Reusable-Rockets`、`Rocket Lab` 这类大小写变体，可能导致文章标签或公司关系静默丢失。现在关系 lookup 会按小写归一去重，并用大小写不敏感 SQL 匹配。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-297`。
- DONE：带聚合器前缀的旧来源筛选兼容映射已完成。来源展示 helper 已经能连续清理链式聚合器前缀，但 `/api/articles?source=...` 的 public source filter 映射此前只做空白和大小写归一；旧链接如 `Google News RSS - RSSHub - 微博商业航天关键词` 可能无法映射回配置 source key。现在筛选映射会复用共享聚合器前缀清理。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-296`。
- DONE：链式聚合器来源前缀显示清理已完成。共享来源显示 helper 此前只剥离最前面一层 `Google News RSS` / `Google News` / `RSSHub` 前缀；历史或未来来源标签如果叠加成 `Google News RSS - RSSHub - 微博商业航天关键词`，仍可能把 `RSSHub` 实现词暴露到前台。现在 helper 会连续清理已知聚合器前缀。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-295`。
- DONE：文章详情关联发射公开边界已补齐。文章详情发射引用此前按原始 `id/externalId/name` key 去重并保留首个对象；历史 payload 如果存在 external id 大小写漂移，或首个引用只有 fallback 标签、后续重复项带有可展示任务名，详情页仍可能出现重复发射 chip 或保留低质量 fallback 标签。现在公开发射引用序列化会按大小写不敏感 key 去重，并在后续重复项提供可用任务名时替换 fallback 标签。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-294`。
- DONE：文章公开实体引用边界已补齐。聚合层已经会合并并改善 `tags/companies`，但公开文章序列化此前仍按精确 slug 去重并保留首个实体对象；历史关系 payload 如果出现 `satellite-internet` / `Satellite-Internet`，或首个同 slug 名称为空白、后续重复项带有可展示名称，公开 API 仍可能输出重复 chip 或退回 slug 标签。现在公开实体序列化会按大小写不敏感 slug 去重，并在后续重复项提供可用名称时替换 fallback 标签。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-293`。
- DONE：文章聚合实体标签质量已补齐。`SI-ISSUE-291` 已让重复故事行合并 `tags/companies`，但重复 slug 此前保留首个实体对象；如果首个对象只有有效 slug、名称为空白，而后续重复行带有可展示名称，聚合后的文章列表仍可能把 chip 名称退回 slug。现在实体合并会保存裁剪后的 slug，并在重复 slug 上用后续可用名称补齐空白名称。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-292`。
- DONE：文章聚合层实体关系保留已补齐。同一故事聚合时会保留最新文章行，并已携带相关来源列表，但此前没有合并较早重复行的 `tags/companies`；如果旧来源识别到了公司或专题，而较新的重复来源没有实体关系，文章卡片会丢掉已有实体 chip。现在聚合层会按清理后的 slug 合并专题和公司引用，同时继续保留最新文章的标题、URL、发布时间和来源显示字段。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-291`。
- DONE：文章聚合层相关来源去重已前移。公开文章 API 和前端显示已经会在清理聚合器前缀、大小写和空白后去重，但 `clusterArticleRows` 此前仍用原始字符串判断重复来源；同一故事如果出现 `Spaceflight News API`、`spaceflight news api`、`Google News RSS - Spaceflight News API`，会先在聚合层夸大 `relatedSourceCount`，再依赖公开层兜底修正。现在聚合层已改为复用共享来源清理规则和大小写不敏感键去重，并保留首次出现的来源展示写法。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-290`。
- DONE：文章相关来源显示已补齐清理后去重。公开文章 API 已经对 `relatedSources` 做去重，前端兼容 helper 也已经清理聚合器前缀、压缩空白和丢弃空标签，但旧 payload 中 `SpaceNews`、`spacenews`、`Google News RSS - SpaceNews` 这类清理后等价来源仍可能在文章卡片和详情页重复显示。现在相关来源显示 helper 会在清理后按大小写不敏感键去重，再应用展示上限。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-289`。
- DONE：文章标题和摘要显示已补齐前端展示归一化。公开文章 API 已经清理标题、原文标题、摘要和原文摘要，但 `articleFromApi`、文章卡片和文章详情此前仍直接渲染原始标题/摘要字段；旧 payload 中 ` Reusable   rocket\tmilestone ` 或 ` Short\nsummary   only. ` 会污染标题、摘要、aria label 和原文信息块。现在文章可见文本共用文章文本显示 helper，并保留中文兜底、来源 URL 和路由行为。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-288`。
- DONE：文章相关来源显示已补齐前端展示归一化。公开文章 API 已经清理 `relatedSources`，但文章卡片和文章详情此前仍直接 join `item.relatedSources` / `article.relatedSources`；旧 payload 中 `Google News RSS - 商业   航天`、带空白来源名或空条目会在多源覆盖展示里暴露聚合器前缀或脏空白。现在相关来源展示共用 `displayRelatedSourceNames`，会清理聚合器前缀、压缩空白、丢弃空标签并保留原有最多展示 4 个来源的边界。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-287`。
- DONE：文章实体 chip 已补齐前端展示归一化。公开文章 API 已经清理嵌套公司、专题和发射标签，但文章卡片此前仍直接渲染 `company.name` / `tag.name`，文章详情 helper 也返回原始嵌套实体标签；旧 payload 中 ` Rocket   Lab `、` 可回收   火箭 ` 或 ` Demo   launch ` 会显示脏 chip。现在文章卡片和详情实体 chip 复用共享展示归一化，路由 slug 行为保持不变。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-286`。
- DONE：顶部命令菜单可见标签已补齐前端展示归一化。搜索路径、公司页、专题页和文章筛选建议已经清理可见实体文本，但命令菜单此前仍直接渲染 `commandQuery.trim()`、`company.name` 和 `topic.name`；旧 payload 或输入中的 ` Rocket   Lab `、` 可回收   火箭 ` 会显示脏命令标签。现在命令菜单搜索标签会压缩内部空白，公司/专题命令标签复用共享展示 helper，命令 key 改用目标路由避免归一化后碰撞。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-285`。
- DONE：文章高级筛选建议项已补齐前端展示归一化。公司和专题列表/详情已经清理可见名称，但 `ArticleFilterPanel` 此前仍把原始 `topic.name` 和 `company.name` 写入 datalist 选项；旧 payload 中 ` 可回收   火箭 ` 或 ` Rocket   Lab ` 会显示脏建议项。现在专题/公司筛选建议复用 `displayTopicName` 和 `displayCompanyName`。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-284`。
- DONE：来源筛选选项已补齐前端展示归一化。公开来源 API 已经清理来源名、分类标签和公开徽标，但 `SourceOptions` 此前仍用精确 `categoryLabel` 匹配并直接渲染 `source.name` / `source.publicBadge`；旧 payload 中 ` 官方   机构 ` 或 ` 国家   航天局 ` 会隐藏政策筛选来源或显示脏选项。现在来源选项会压缩分类标签后匹配，并清理来源名与公开徽标。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-283`。
- DONE：专题名称和分类显示已复用前端展示归一化。公开专题 API 已经清理专题名称和分类标签，但专题列表卡片与专题详情标题此前仍直接渲染 `topic.name`、`categoryLabel` 和 `state.data.name`；旧 payload 中 ` 可回收   火箭 ` 或 ` 技术   路线 ` 会显示脏标签。现在这些入口共用 `displayTopicName` / `displayTopicCategory`，目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-282`。
- DONE：公司元数据显示已复用前端展示归一化。公开公司 API 已经清理国家、赛道和股票代码，但公司列表卡片与公司详情元信息此前仍直接渲染 `countryLabel`、`sectorLabel` 和 `stockSymbol`；旧 payload 中 ` 美   国 `、` 发射   服务 ` 或 ` NASDAQ:   RKLB ` 会显示脏元数据。现在这些入口共用 `displayCompanyMetadata`，目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-281`。
- DONE：公司名称显示已复用前端展示归一化。公开公司 API 已经清理公司名称，但公司列表卡片和公司详情标题此前仍直接渲染 `company.name` / `state.data.name`；旧 payload 中 ` Rocket   Lab ` 会显示脏标题。现在这些入口共用 `displayCompanyName`，目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-280`。
- DONE：公司详情简介显示已收敛内部空白。公开公司 API 已经清理简介文本，但公司详情页此前仍直接渲染 `state.data.profile`；旧 payload 中带换行或连续空格的简介会显示不稳定。现在详情页渲染前会压缩连续空白，空简介仍使用“暂无公开简介。”兜底。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-279`。
- DONE：专题详情精选资料备注显示已收敛内部空白。公开专题 API 已经清理精选资料备注，但专题详情页本地 `curationLabel` 此前只做 trim；旧 payload 中 ` 精选   资料 ` 会显示脏空白。现在备注渲染前会压缩连续空白，空备注仍使用域名兜底。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-278`。
- DONE：发射任务名显示已复用公开文本归一化。公开发射 API 已经清理任务名字段，但发射详情标题、发射列表卡片和首页 HUD 此前仍直接渲染 `launch.mission`；旧 payload 中 ` Demo   launch ` 会显示脏空白。现在这些入口共用 `displayLaunchMission`，同时保留现有兜底文案。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-277`。
- DONE：发射火箭型号和场站显示已复用公开文本归一化。公开发射 API 已经清理火箭和场站字段，但发射详情与发射列表此前仍直接渲染 `launch.rocket` 和 `launch.site`；旧 payload 中 ` Falcon   9 ` 或 ` Cape   Canaveral ` 会显示脏空白。现在这些入口共用发射元数据显示 helper，保留原有中文兜底。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-276`。
- DONE：发射商显示已复用公开文本归一化。公开发射 API 已经清理发射商字段，筛选链接 helper 也会压缩内部空白，但发射详情、发射列表和首页 HUD 此前仍直接渲染 `launch.provider`；旧 payload 中 ` Rocket   Lab ` 会显示脏文本，同时链接却指向清理后的 `provider=Rocket+Lab`。现在这些入口共用 `displayLaunchProvider`，占位发射商仍显示兜底且不生成筛选链接。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-275`。
- DONE：文章详情页地区标签已复用公开显示归一化。文章列表卡片已经把 `regionLabel` 归一为“国内/国际/地区待确认”，但详情页此前直接渲染并链接原始 `article.regionLabel`，旧 payload 中 `国   内` 这类空白变体会显示脏文本且无法进入地区筛选。现在详情页显示和地区筛选 helper 都复用 `displayRegion` 归一化。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-274`。
- DONE：文章详情页来源筛选入口已复用真实发布方判断。`SI-ISSUE-272` 已修复文章卡片映射，但详情页仍用原始 `publisherName ? undefined : sourceName` 判断来源是否可点击，旧 payload 中 `Google News RSS -    ` 仍会抑制详情页来源筛选。现在详情页和卡片共用 `articleSourceFilterValue`，清理后没有真实发布方时保留来源筛选入口。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-273`。
- DONE：旧发布方标签不再误抑制来源筛选入口。`articleFromApi` 此前只看原始 `publisherName` 是否 truthy，旧 payload 中 `Google News RSS -    ` 或与来源名重复的发布方标签会被当成真实发布方，导致文章卡片/详情无法进入来源筛选。现在前端会在聚合器前缀清理和来源标签归一化后判断发布方是否真实不同；空白或来源型发布方保留来源筛选入口，真实媒体发布方仍不生成误导筛选。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-272`。
- DONE：文章卡片分类映射已收敛公开标签空白变体。`articleFromApi` 会把公开 `sourceCategoryLabel/regionLabel` 映射成文章卡片分类，但此前依赖精确标签，旧 payload 或异常值如 `官方   机构`、`国   内`、`国   际` 可能退成通用“商业航天”。现在卡片映射会复用地区显示归一化，并用去空白紧凑键识别政策类公开标签。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-271`。
- DONE：筛选跳转占位标签判断已收紧。`SI-ISSUE-269` 让筛选跳转参数压缩内部空白后，`来   源`、`发射商   待定` 这类占位标签会变成 `来 源`、`发射商 待定`，仍可能绕过精确占位判断并生成无效筛选链接。现在来源和发射商 pivot-link helper 使用去空白紧凑键判断占位值，真实筛选链接仍使用规范化后的展示值。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-270`。
- DONE：筛选跳转链接已收敛内部空白。共享表单和分页参数已经会压缩内部空白，但文章来源、公司、专题和发射商等 pivot-link helper 仍只做 trim，旧 payload 或异常标签可能生成 `Spaceflight+++Now`、`Rocket+++Lab` 这类噪声筛选链接并降低命中。现在这些筛选跳转入口复用同一套参数清理，仍会丢弃空白和占位标签。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-269`。
- DONE：前端搜索和筛选 URL 已收敛内部空白。共享 URL 参数 helper 和页头搜索此前只做 trim，`Rocket   Lab` 这类输入会生成带多空格的 query/API path，降低搜索命中并产生噪声筛选 URL。现在前端搜索、筛选表单、分页保留参数和页头搜索路径都会把连续空白压缩为单空格，同时继续丢弃全空值。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-268`。
- DONE：文章地区公开显示已收敛内部空白。文章地区筛选别名已经兼容 `国   内`、`国   际` 等空白变体，但公开显示 helper 仍只做 trim 和小写，历史 region 值可能显示成“地区待确认”。现在地区显示会压缩内部空白，并用紧凑中文标签把 `国   内` 归为“国内”、`国   际/国   外` 归为“国际”。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-267`。
- DONE：发射状态公开显示已收敛内部空白。发射状态筛选已经收敛 `No   Go`、`不   成功` 等空白变体，但公开显示 helper 仍只做小写判断，历史或上游状态值可能把 `No   Go` 显示成“准备发射”、把 `不   成功` 显示成“发射成功”。现在发射状态显示会压缩内部空白，并用紧凑中文状态键和 spaced `No Go` 变体匹配公开中文状态。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-266`。
- DONE：文章地区和分类筛选别名已收敛内部空白。文章筛选已经支持“国内”“国际”“政策监管”“官方机构”等公开文案，但别名匹配前只做 trim 和小写，`国   内`、`政策   监管` 这类 URL 或表单值可能绕过映射并作为原始值进入查询。现在文章地区/分类筛选会压缩连续空白，并用紧凑中文标签匹配别名，未知筛选仍保留规范化回退。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-265`。
- DONE：来源筛选映射已收敛内部空白。来源公开名和聚合器前缀清理已经会压缩展示空白，但来源筛选值映射到 source key 时只做 trim 和小写，`Spaceflight   News` 或 `RSSHub  -   微博商业航天关键词` 这类旧链接/手输值可能绕过兼容映射。现在来源筛选映射会对配置公开名、原始来源名、source key 和输入值统一压缩连续空白，未知筛选仍保留原有 trim 后回退。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-264`。
- DONE：发射状态筛选别名已收敛内部空白。发射筛选已经支持“准备发射”“No Go”“不成功”等公开状态文案，但匹配前只做 trim 和小写，`No   Go` 可能被误判为准备发射，`不   成功` 也可能先命中成功别名。现在发射状态筛选会在别名匹配前压缩内部空白，并用紧凑中文状态键防止带空白的否定状态被误判。目标测试、完整回归、类型检查、lint、配置检查、构建、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-263`。
- DONE：文章详情关联发射引用已按公开路由标识去重。关联发射名称已经会清理展示文本，但详情 payload 仍会保留重复 join/关系行返回的同一发射，页面可能出现重复发射 chip 和重复 React key。现在文章详情公开序列化会在清理后按 `id`、`externalId`、展示名的路由优先级去重，保留首个公开发射标签。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-262`。
- DONE：文章嵌套公司和专题引用已按公开 slug 去重。嵌套实体引用已经会丢弃空 slug 并清理展示名，但清理后如果同一 slug 重复出现，文章卡片和详情页仍可能显示重复标签。现在文章公开序列化会按清理后的 slug 对嵌套公司/专题引用去重，保留首个公开展示名，避免历史关系数据或回填漂移造成重复 chip。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-261`。
- DONE：文章嵌套实体和发射引用标签内部空白已收敛。文章标题、摘要、来源和相关来源标签此前已经压缩连续空白，但文章卡片和详情页中的公司/专题标签、关联发射名称仍只做 trim；历史关系数据或回填数据如果包含换行、制表符或连续空格，会继续造成标签显示不稳定。现在文章公开序列化会压缩嵌套公司/专题名称和文章详情发射引用名称的连续空白，同时保留 slug、external id 等路由字段的原有边界。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-260`。
- DONE：专题详情精选资料数量已和公开条目对齐。`SI-ISSUE-258` 已让无效精选资料不再出现在专题详情公开 payload 中，但详情响应的 `curationCount` 仍可能来自原始数据库数量，出现只返回 1 条可打开精选资料却显示 3 条的契约不一致。现在专题详情序列化会用实际返回的公开精选资料数组计算 `curationCount`，保持数量和条目一致。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-259`。
- DONE：专题详情公开 API 已丢弃不可打开的精选资料。`SI-ISSUE-257` 已把专题精选资料中的 unsafe 或空白 `itemUrl` 归一为 `null`，前端也会过滤这些链接；但公开 API 仍可能返回不可点击的精选资料对象，让调用方继续承担“真实精选资料”和“历史脏数据”的区分成本。现在专题精选资料公开序列化会直接丢弃空白或非 `http/https` URL 的条目，返回的精选资料 URL 恢复为必定可打开的字符串。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-258`。
- DONE：公司和专题公开 URL 协议边界已对齐。文章和发射公开 API 已把 unsafe URL 归一为 `null`，但公司 `website/logoUrl` 与专题精选资料 `itemUrl` 仍只 trim；虽然前端会再次用 `safeExternalUrl` 防御，公开 API 本身仍可能把历史 DB 中的 `javascript:`、`data:` 或空白链接返回给调用方。现在公司官网、Logo 和专题精选资料 URL 会在公开序列化层复用 `normalizeHttpUrl`，非 `http/https` 或空白值返回 `null`。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-257`。
- DONE：公司、专题和发射公开文本内部空白已收敛。`SI-ISSUE-255` 已收敛文章标题和摘要，但公司名称/简介、专题名称/精选备注、发射任务/火箭/发射商/场站等公开展示字段仍只做 trim；历史数据或人工数据如果包含换行、制表符或连续空格，会在目录、详情和发射页显示不稳定文本。现在这些公开展示字段会把连续空白压缩为单空格；slug、URL、外部 ID 等路由或链接字段仍保留原有边界。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-256`。
- DONE：文章公开 API 标题和摘要内部空白已收敛。`SI-ISSUE-253` 和 `SI-ISSUE-254` 已收敛来源类公开标签，但文章标题、摘要、原文标题和原文摘要仍只做 trim；历史 RSS、HTML 摘要或人工修正如果包含换行、制表符或连续空格，资讯卡片和详情页可能显示不稳定文本。现在公开文章文本字段会把连续空白压缩为单空格后再应用兜底或 nullable 规则。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-255`。
- DONE：共享来源展示元数据内部空白已收敛。`SI-ISSUE-253` 已让文章公开 API 的来源、发布方和相关来源标签压缩连续空白，但更上游的共享 `sourceDisplayName`、访问说明和公开徽标仍只做 trim；后续新增来源如果写入 `Spaceflight  News`、`国内访问   可能受限` 这类文本，来源目录、筛选选项、首页来源状态和 enabled 来源唯一性校验仍可能出现不稳定展示或重复名称漏检。现在共享来源展示 helper 会压缩连续空白，enabled 来源公开名唯一性校验也会使用收敛后的展示名。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-254`。
- DONE：文章公开 API 来源展示标签内部空白已收敛。前后空白、空发布方、大小写漂移和聚合器前缀已经在 `SI-ISSUE-248` 到 `SI-ISSUE-252` 中收敛，但公开来源、发布方和相关来源标签仍只做 trim，不压缩中间连续空白；历史 RSS 或人工数据如果包含制表符、多空格，页面可能显示不够成熟的来源名称，并让 `Spaceflight  Now` / `Spaceflight Now` 被计为不同相关来源。现在公开展示标签会在清理聚合器前缀后压缩连续空白，再进入发布方重复判断和相关来源去重。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-253`。
- DONE：文章公开 API 发布方重复判断大小写已收敛。`SI-ISSUE-249` 和 `SI-ISSUE-250` 已把等于来源名或来源兜底名的发布方归一为 `null`，但比较仍是大小写敏感；历史数据或不同采集器可能保存 `Spaceflight News` / `spaceflight news`、`Google News` / `google news` 这类大小写漂移，导致重复发布方继续被当成独立发布方并抑制来源筛选入口；现在复用公开标签大小写不敏感键做判断。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-252`。
- DONE：文章公开 API 相关来源大小写去重已收敛。公开 `relatedSources` 此前按精确字符串去重，历史聚合或不同采集器可能产生 `SpaceNews` / `spacenews`、`Spaceflight Now` / `spaceflight now` 这类大小写漂移，导致同一来源被计为两个公开来源并夸大“多源覆盖”；现在按大小写不敏感键去重，同时保留首次出现的展示写法。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-251`。
- DONE：文章公开 API 聚合来源兜底发布方已收敛。Google News RSS 如果解析不到真实媒体发布方，采集层会用配置来源名兜底；历史数据也可能保存了 `Google News RSS - 商业航天` 或 `Google News - 商业航天` 这类来源型发布方。公开层此前会把它显示成“商业航天”，看起来像真实发布方并抑制来源筛选入口；现在清理后等于原始来源兜底名的发布方会归一为 `null`，真正不同的媒体发布方仍保留。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-250`。
- DONE：文章公开 API 重复发布方标签已收敛。RSS、官方页和采购页采集器会把 `publisherName` 设置为与来源名相同，这个值没有提供额外信息；但前端会把非空 `publisherName` 当作独立发布方，从而不生成配置来源筛选入口；现在公开序列化层会把清理后等于公开来源名的发布方归一为 `null`，让前端展示来源名并保留来源筛选入口。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-249`。
- DONE：文章公开 API 空发布方标签已收敛。来源名可以使用“来源”作为兜底，但发布方 `publisherName` 是可选字段；此前序列化层会把 `Google News RSS -    ` 这类清理后为空的发布方变成通用“来源”，可能让详情页显示低质量来源文案并抑制配置来源筛选入口；现在清理后为空的发布方返回 `null`，由前端回退到配置来源名。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-248`。
- DONE：文章公开 API 全空相关来源计数已收敛。`SI-ISSUE-246` 已让清理后只剩一个公开来源名的 payload 不再暴露单项 `relatedSources`，但如果原始 `relatedSources` 字段存在且所有条目清理后为空，序列化层仍会回退旧 `relatedSourceCount`，可能把没有公开来源名的数据继续显示为多源覆盖；现在序列化层会区分未提供列表和已提供但清理为空的列表，后者按 0 个公开相关来源处理。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-247`。
- DONE：文章公开 API 相关来源明细已收敛。`SI-ISSUE-245` 已在详情页防御单来源 payload 的冗余展示，但公开文章序列化仍可能在聚合器前缀清理和去重后只剩一个来源名时返回单项 `relatedSources`，让 API payload 自身继续表达成“有相关来源列表”；现在 API 只在清理后超过一个来源时暴露明细，同时保留清理后的覆盖计数。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-246`。
- DONE：文章详情页相关来源块已避免单来源冗余。`SI-ISSUE-244` 补齐多源覆盖来源明细后，仍需防御旧 payload 或异常 payload 在 `relatedSourceCount` 为 1 时携带 `relatedSources` 的情况，避免单来源详情也显示“相关来源”块；现在相关来源明细只在多源覆盖时展示。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-245`。
- DONE：文章详情页多源覆盖来源明细已补齐。文章卡片在多源覆盖时会展示相关来源列表，但文章详情页此前只显示“X 源覆盖”的数量，不展示具体来源，用户进入详情后反而少了来源判断信息；现在详情页会补上已清理后的相关来源明细。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-244`。
- DONE：首页来源状态卡片访问兜底已收敛。来源状态 API 与首页兜底现在都会给每个分类返回 `accessSummaryLabel`，但前端旧兜底逻辑在缺少单分类访问摘要时会把全站 `accessStats` 拼到每个分类卡片下，容易把全站访问分布误读为该分类访问状态；现在缺少分类摘要时显示“待验证”。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-243`。
- DONE：筛选表单提交空白参数处理已收敛。资讯高级筛选、政策筛选和发射筛选此前依赖浏览器 GET 表单提交，空白输入或空选择可能生成带 `query=+++`、`source=`、`provider=` 等空值的公开 URL；现在这些筛选表单接入共享路径清理规则，只保留非空白的公开筛选字段。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和运行时代码敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-242`。
- DONE：顶部可见搜索表单空白查询处理已收敛。命令面板搜索已会裁剪关键词并丢弃空输入，但页头可见搜索框提交时仍可能把空白关键词写入 URL，产生 `/articles?query=+++` 这类噪声筛选状态；现在可见搜索表单复用同一搜索路径规则，空白搜索回到资讯列表，非空搜索保持裁剪后的公开查询。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和本轮改动敏感信息检查已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-241`。
- DONE：公司/专题详情页相关文章空态已避免和完整筛选入口冲突。`SI-ISSUE-238` 增加完整文章筛选入口后，仍存在防御性边界：如果详情记录 `articleCount > 0` 但内嵌相关文章数组为空，页面可能同时显示“暂无相关新闻”和“查看全部”。现在空态只在总数为 0 且无内嵌文章时显示，正数总数会保留完整筛选入口。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-240`。
- IN_PROGRESS：生产来源 API 仍需部署闭环。2026-05-31 复查 `https://space.bytebaud.com/api/sources`，线上仍返回旧响应形态，包含 `key/type/region/credibility/publicCategory/accessDomestic/accessGlobal` 和顶层 `stats` 等内部字段；本地代码在 `SI-ISSUE-230` 已修复并验证，但生产端点未匹配本地公开契约，不能视为线上完成。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-239`。
- DONE：公司/专题详情页已补完整文章筛选入口。详情接口只内嵌最近相关文章，但文章列表已支持 `company` 和 `tag` 筛选；此前当相关数量超过详情页预览数量时，用户没有直接进入完整筛选结果的入口。现在公司和专题详情会在 `articleCount` 超过已展示数量时显示对应 `/articles?company=...` 或 `/articles?tag=...` 链接，预览已完整时不显示冗余入口。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-238`。
- DONE：发射时间线分页状态已接入前端。发射 API 已支持 `page/limit/hasMore`，但发射列表此前只请求固定 `limit=12` 且没有上一页/下一页控件，URL 页码不会影响结果；现在页面会清理并传递 `page`、保留公开筛选参数，渲染发射分页导航。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-237`。
- DONE：发射详情页发射商元信息已支持筛选跳转。发射列表已有 `provider` 筛选，但详情页发射商此前只是纯文本；现在可用发射商会链接到 `/launches?provider=公开发射商`，缺失或待定发射商仍保持纯文本，避免空筛选或误导筛选。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-236`。
- DONE：文章详情页元信息已支持精确筛选跳转。详情页来源和地区此前只是纯文本；现在配置来源和国内/国际地区会链接到现有文章筛选，未知地区、兜底来源和聚合发布方仍保持纯文本，避免空筛选或误导筛选。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-235`。
- DONE：首页来源状态分类链接已变得更精确。`LiveHud` 来源状态卡片此前全部跳转 `/articles`，即使“官方机构/公告信息”已经能准确对应现有政策筛选；现在这两类跳转 `/articles?category=policy`，专业媒体、数据来源、来源等更宽泛分类仍跳资讯总表，避免制造不支持的假筛选。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-234`。
- DONE：新闻卡片来源名已可直接进入来源筛选。文章卡片地区标签已可点，但来源名此前只是普通文本；现在有明确配置来源筛选值时会链接到 `/articles?source=公开来源名`，复用现有公开来源筛选，不暴露 source key，同时保留“来源”兜底和聚合发布方文本不生成无效筛选。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-233`。
- DONE：首页来源状态兜底已补访问标识。`LiveHud` 在 `/api/sources` 加载前使用 `/api/home` 的来源分类统计，但旧兜底只有分类和数量，可能把包含国内访问受限或待验证来源的分类临时显示为“可用”；现在 home stats 查询带出 region，公开序列化按配置访问元数据或地区兜底生成 `accessSummaryLabel`，前端兜底也显示“可能受限/待验证/部分受限”等公开访问状态。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-232`。
- DONE：首页来源状态兜底排序已对齐来源 API。`LiveHud` 在 `/api/sources` 加载前会使用 `/api/home` 的 `enabledSourceCategories`，此前该兜底统计按内部 enabled source 查询顺序聚合，可能和来源状态 API 的公开产品顺序不一致；现在公开来源分类排序和访问状态排序已收敛到 `sourceDisplay` 共享 helper，首页兜底和来源 API 使用同一套顺序。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-231`。
- DONE：来源状态 API 公开排序已稳定。复查 `/api/sources` 发现本地代码虽已隐藏 key、type、region、stats 等内部字段，但公开来源列表和统计顺序仍继承数据库/内部来源顺序，后续新增来源时用户看到的来源状态容易显得像实现细节；现在来源列表按官方机构、专业媒体、行业组织、公告信息、数据来源、来源排序，访问状态按直连、可能受限、受限、待验证排序。目标测试、完整回归、类型检查、lint、配置检查、构建、布局验证、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-230`。
- DONE：布局验证脚本已防止 stale dist 假通过。`verify-layout` 默认启动当前 `dist` 后，仍可能在源码已变但未重新构建时检查旧产物；脚本现在未设置 `PLAYWRIGHT_TARGET_URL` 时会先运行 Vite build，再启动 preview 做 desktop/tablet/mobile 检查。`verify-layout`、完整回归、类型检查、lint、配置检查、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-229`。
- DONE：布局验证脚本已改为默认验证当前构建产物。复查发现 `node scripts\verify-layout.mjs` 默认打到 `127.0.0.1:5173`，如果该端口运行的是旧 dev server，会产生 `policySignal=false/policyPage=false` 这类假失败；脚本现在在未显式设置 `PLAYWRIGHT_TARGET_URL` 时自动启动当前 `dist` 的 Vite preview 并在结束时关闭。`verify-layout` desktop/tablet/mobile、lint、完整回归、类型检查、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-228`。
- DONE：官网/采购页相关性泛词已收紧。真实 enabled 来源复查发现 `most-news`、`beijing-jxj-notices`、`ccgp-central-procurement` 等会因“重大项目/产业基金/申报/通知/公示/意见/规划/行动方案”等非航天词或同块上下文误收无关科技、政务、采购信息；默认官网相关词、北京来源 include terms 和采购页领域信号判断已收紧为必须出现航天/卫星/火箭等行业信号。目标测试、真实来源复查、配置生成检查、完整回归、类型检查、lint、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-227`。
- DONE：官方页默认相关性词表已移除泛化“新闻发布会”。真实 `miit-news` 复查发现综合时政列表会因为同一大块上下文里存在“新闻发布会”而把无关国务院、会议、汽车、通信等新闻判为航天相关；默认相关词已移除此泛词，保留“商业航天/卫星/行动方案”等行业词命中真实航天发布会。目标测试、真实 MIIT 复查、完整回归、类型检查、lint、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-226`。
- DONE：官网/公告页短入口和占位 href 噪声继续收紧。真实来源复查发现 `miit-news`、`cmse-news`、`ccgp-central-procurement`、`landspace-news`、`spacepioneer-news`、`cas-space-news`、`deepblueaerospace-news`、`changguang-satellite-news` 等仍会产出导航、产品、招聘、分享、法务和 `{javascript:;}` 占位链接候选；共享 HTML 抽取已拒绝占位 href，并补充已取证精确短标题过滤。目标测试、真实来源复查、完整回归、类型检查、lint、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-225`。
- DONE：公司官网产品/服务入口候选已收紧。真实来源复查发现 `orienspace-news` 的“引力火箭”、`cas-space-news` 的“产品及服务/发射服务/力箭系列火箭”、`deepblueaerospace-news` 的 `Product/Engine/Launch Vehicle/Launch` 等短产品或服务入口仍可能因航天关键词被当成文章候选；共享短标题过滤已补充已取证精确标题，并保留真实长文章标题。目标测试、真实公司源复查、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-224`。
- DONE：官方页同源限制误伤外部文章链接已修正。复查 `orienspace-news` 发现多条真实公司新闻指向 `mp.weixin.qq.com`，`cas-space-news` 和 `changguang-satellite-news` 也存在外部文章/媒体报道链接，上一轮 `official_page` blanket 同源限制会丢失这些真实候选；现在已改为精确过滤已取证页脚/服务/备案链接，同时保留外部文章链接。目标测试、真实来源复查、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-223`。
- DONE：官方页外部友情链接候选已收紧。真实 `cnsa-news` 页面复查发现正文文章之后还会抽出中央政府、工信部、探月网等页脚外部链接；共享短标题过滤已补充这些页脚/服务/备案链接的精确过滤，CNSA 复查只剩同源真实文章，同时不禁止其他来源的外部文章链接。目标测试、真实 CNSA 复查、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-222`。
- DONE：脚本内伪链接抽取已修复。真实 `cnsa-news` 页面复查发现分页脚本里的伪 `<a href="+url+">"+(...)+ "</a>` 会被正则当成真实链接候选；共享 HTML 抽取现在会在扫描列表和锚点前移除 `script/style` 内容。目标测试、真实 CNSA 复查、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-221`。
- DONE：HTML 卡片锚点标题已改为优先读取 `title` 属性。真实 `cnsa-news` 页面复查发现文章卡片的 `<a>` 包住标题、日期和摘要，旧抽取会把整段卡片文本当成公开标题；共享抽取层现在优先使用锚点 `title` 属性作为候选标题，并保留整块上下文用于日期提取。目标测试、真实 CNSA 复查、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-220`。
- DONE：CNSA 栏目入口抽取噪声已收紧。真实 `cnsa-news` 页面复查发现共享 HTML 抽取会产出“信息发布”“国际合作”“图解航天”“> 政策公告”等栏目入口；共享短标题过滤现在会归一化 breadcrumb 标记并过滤已取证 CNSA 栏目/服务短标签。真实 CNSA 复查只剩同源真实文章候选，目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-219`。
- DONE：HTML 日期提取已改为跳过非法候选继续扫描。共享 `extractDate` 原来遇到第一个年在前的日期形状但日期非法时会直接返回 `null`，不会继续读取后续有效源站日期，可能让带脏页面上下文的官方页/采购页记录回退采集时间；现在会扫描后续有效日期候选，再进入 slash 日期兜底。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-218`。
- DONE：独立文章锚点相邻日期上下文已补齐。enabled 官网复查发现部分卡片结构把日期放在 `<a>` 外侧，旧独立锚点召回只把锚点文本作为 `contextText`，会让 `cas-space-news`、`casic-news` 这类文章明明有源站日期却回退采集时间；共享 HTML 抽取层现在为独立锚点补最近日期上下文，同时保留列表/表格块上下文优先级。目标测试、真实来源抽样复查、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-217`。
- DONE：slash 日期解析已补充月/日/年兜底。`orienspace-news` 当前存在 `12/31/2025` 这种不能按日/月/年解析的日期，旧逻辑会回退采集时间；共享日期提取器现在仍优先日/月/年，但在真实日期校验失败时再尝试月/日/年。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-216`。
- DONE：HTML 抽取已过滤日期型和详情型短链接。enabled 来源复查发现 `[2026-05-28]`、`2026/03/30`、`[查看全部]`、`【详情】` 这类短链接会指向文章 URL，并可能在 URL 去重前抢占真实文章标题；共享抽取层现在会过滤日期-only 和详情/查看全部短标题。目标测试、真实来源复查、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-215`。
- DONE：官方页标题开头日期已从公开标题中清理。`SI-ISSUE-213` 让 `orienspace-news` 的 `DD/MM/YYYY` 日期能被解析，但源站把日期拼在标题开头，公开标题会显得像抓取痕迹；官方页采集现在把开头日期用于发布时间，同时从公开 `title/summary` 中移除，并保留 `originalTitle` 追溯原文。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-214`。
- DONE：HTML 日期解析已补充日/月/年格式。独立文章卡片审计发现 `orienspace-news` 标题使用 `11/10/2025`、`13/09/2025` 这类 `DD/MM/YYYY` 日期，旧解析只支持年在前格式，会把发布时间回退到采集时间；共享日期提取器已补充真实日期校验。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-213`。
- DONE：独立锚点召回后的栏目/产品入口噪声已收紧。`SI-ISSUE-211` 提高真实文章召回后，enabled 来源复查发现“航天文化”“航天科普”“预定发射”“力箭一号运载火箭”等非文章入口也可能被相关性词命中；共享抽取层已按已取证短标题做精确过滤。目标测试、真实来源噪声复查、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-212`。
- DONE：HTML 抽取已补充独立文章卡片链接召回。enabled 官网结构审计发现部分页面同时存在导航列表和独立文章卡片，旧逻辑在发现 `<li>/<tr>` 后会忽略列表外真实文章链接；共享抽取层现在会同时扫描独立 `<a>` 块，并按 URL+标题去重。真实来源复查、目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-211`。
- DONE：HTML 列表抽取已过滤短栏目链接。`SI-ISSUE-209` 让同块内后续文章链接能进入候选，但同块前置的“通知公告”“政策文件”“采购公告”等栏目链接也可能借同块上下文误判相关；共享抽取层现在会跳过常见短栏目/导航标题，只保留真实文章候选。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-210`。
- DONE：HTML 列表块已抽取同块内所有可用链接。`extractHtmlListLinks` 原来每个 `<li>/<tr>` 只取第一个 `<a>`，政务列表中如果先放栏目链接、后放文章链接，会抓到栏目后错过真正文章；现在同一列表块内的全部可用网页链接都会进入候选集，后续导航和相关性过滤继续负责筛除栏目项。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-209`。
- DONE：HTML 常见排版实体解码已补齐。enabled 来源抽样发现 `ccgp-central-procurement` 含 `&raquo;`，`spacepioneer-news`/`cas-space-news` 含 `&ldquo;`、`&rdquo;`，这些实体可能残留到标题或上下文；共享 `decodeHtml` 现在支持引号、书名号、长短横线、中点和版权符号等常见命名实体。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-208`。
- DONE：官方页/采购页日期提取已校验真实日期。共享 `extractDate` 原本只按正则拼接日期字符串，`2026-13-40` 或 `2026-02-30` 这类非法日期可能进入 `publishedAt`；现在会校验 UTC 年月日一致性，非法日期回退采集时间。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-207`。
- DONE：HTML 命名实体解码已覆盖大小写和 `&apos;`。共享 `decodeHtml` 原来只处理小写 `&amp;`、`&nbsp;` 等少量实体，`&AMP;`、`&NBSP;`、`&apos;` 可能残留到官方页/RSS/Google News 的标题或摘要中；现在命名实体按大小写不敏感处理并支持 apostrophe。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-206`。
- DONE：HTML 数字实体解码已拒绝隐藏控制字符。`&#0;`、`&#x7f;` 这类实体如果被解码会把不可见控制字符写入标题/摘要，排查和展示都更差；共享解码现在对 C0/C1 控制字符保留原实体文本，目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-205`。
- DONE：Google News 标题拆分已兼容带连字符的发布方名称。原解析只接受不含 `-` 的 publisher 片段，`标题 - 示例媒体-中文站` 这类标题会拆分失败并把来源名留在文章标题里；现在按最后一个 ` - ` 分隔并继续解码实体。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-204`。
- DONE：RSS/Google News 标题展示文本已解码实体。feed channel title、RSS item title 和 Google News title/publisher 原先可能保留 CDATA 中的 `&amp;`、`&#183;` 等实体，导致来源名、标题或原题显示脏文本；采集器现在在写入 normalized item 前统一解码这些展示文本。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-203`。
- DONE：RSS 摘要清理已复用 HTML 实体解码。RSS/Google News 摘要清理原本只移除标签，不解码 `&amp;`、`&#183;`、`&#x2014;` 等实体，可能把脏实体文本写入文章摘要；采集器元数据清理现已复用共享 `decodeHtml`，标准 RSS 与 Google News RSS 目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-202`。
- DONE：HTML 列表解析已支持数字实体解码。enabled 官方源抽样发现页面中存在 `&#xa0;` 等数字实体，原解析器只处理少量命名实体和 `&#39;`，可能让十进制/十六进制实体残留在标题或上下文文本中；共享 `decodeHtml` 现在会解码合法数字实体并保留非法实体原文。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-201`。
- DONE：官方页默认相关性词表已收紧。采集器默认关键词中仍有“产业园”“领导调研”这类泛词，未配置 source-specific include terms 的官方页可能把普通园区调研新闻误判为航天相关；默认词表已改为“空天产业园/航天产业园/商业航天产业园/卫星产业园”等行业限定词，并补充回归测试。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-200`。
- DONE：无锡高新区来源关键词已收紧。enabled 来源审计发现 `wuxi-hightech-news` 使用单独的“产业园”作为 include term，当前页面会把“美丽健康产业园”这类非航天园区新闻判为相关；配置已改为“空天产业园”“商业航天产业园”等带行业限定的关键词，并补充配置测试防止再次加入泛化“产业园”。目标测试、来源复查、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-199`。
- DONE：官方页栏目入口过滤已补齐。enabled 官方源审计发现 `miit-news`/`cmse-news` 会把“时政要闻”“空间科学”“专题报道”“工作动态”这类栏目页作为文章候选，采集器现已按精确标题过滤这些栏目入口，避免后续把栏目页写成政策/新闻记录；目标测试、来源复查、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-198`。
- DONE：海南文昌旧专题单页已暂停 scheduled 采集。`hainan-wenchang-space-special` 虽然当前可访问，但页面是旧专场单页且没有可复用文章列表，继续启用会制造“海南覆盖正常”的假象并浪费采集窗口；配置已保留来源说明、标为旧专题停采，并等待确认新的稳定海南列表来源。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-197`。
- DONE：官方页脚本跳转只在空列表页兜底触发。采集器现在会先尝试提取当前页面链接，只有没有任何可用链接时才跟随同源 `window.location` 跳转，避免中国载人航天工程、广东、无锡等正常页面中偶发的脚本片段让采集器误跳到无关路径；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-196`。
- DONE：官方页采集已支持同源脚本跳转。`ndrc-policy` 配置页返回 `window.location.href='./fzggwl/'` 这类脚本跳转时，采集器现在会跟随一跳同源目标页，并用跳转后的 URL 解析相对链接，避免来源请求成功但静默采不到内容；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-195`。
- DONE：确认 403 的海南/四川宽来源已改为禁用观察项。`hainan-gov-news`、`sichuan-gov-policy`、`sichuan-gov-news` 当前从云端采集路径返回 HTTP 403，已保留配置和访问限制说明但暂停 scheduled 采集，避免持续制造生产失败日志。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-194`。
- DONE：官方页采集已过滤通用导航短标题。`cnsa-news` 这类宽来源仍保留，但采集器现在会跳过“意见征集”“咨询建议”“互动交流”等站点导航入口，避免生产政策接口把导航页当作政策资讯展示；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-193`。
- DONE：陈旧采集日志关闭已从 daily 扩展到每次 scheduled run。hourly 采集现在也会把超过 2 小时仍未完成的 `ingestion_logs` 标记为失败并闭合，避免生产偶发超时、Worker 中断或旧版本遗留 open log 长时间堆积并持续污染 `/api/health` 的 `openIngestionLogCount`；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-192`。
- IN_PROGRESS：生产采集健康仍需复查闭环。2026-05-30 公开 `/api/health` 可访问但仍报告 `openIngestionLogCount: 71`，近期失败来源包含 `cnsa-news`、`sichuan-gov-policy`、`hainan-gov-news` 和 `sichuan-gov-news`；本地代码回归已通过，但生产定时采集健康还不能标记完成。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-006` 和 `SI-ISSUE-008`。
- DONE：RSS/RSSHub 采集来源名已在采集层归一化。标准 RSS 与 RSSHub 现在会 trim feed channel 标题，标题为空时回退配置来源名，并用同一清理后的来源名写入 `sourceName/publisherName`，避免带空格或空白 feed 标题进入文章记录后再依赖公开层兜底；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-191`。
- DONE：新闻采集时间字段已在采集层兜底为可解析时间。SNAPI、RSS/RSSHub 和 Google News RSS 现在会保留可解析发布时间，遇到空白或不可解析发布时间则使用采集时刻，避免新采集文章把 `not-a-date` 这类脏字符串写入数据库后再依赖页面显示“时间待定”；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-190`。
- DONE：新闻采集器已过滤空白标题记录。SNAPI、RSS/RSSHub 和 Google News RSS 现在会在采集层丢弃 trim 后没有可用标题的条目，并把保留记录的标题写入前统一 trim，避免空白标题入库后依赖公开 API 显示“标题待确认”兜底；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-189`。
- DONE：发射缓存写入已按归一化外部 ID 去重。`persistLaunchRecords` 现在会先 trim/lowercase Launch Library 外部 ID，丢弃空白 ID，并在同一批次内只保留首个归一化 ID 对应的发射记录，避免上游重复或大小写漂移让 D1 batch 做重复 upsert、采集成功数虚高或覆盖顺序不稳定；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-188`。
- DONE：文章关联发射外部 ID 已统一归一化。发射缓存写入和文章 `relatedLaunchIds` 关系写入都会 trim 并统一小写外部 ID，文章详情读取相关发射时也用大小写不敏感 join，避免 SNAPI、Launch Library 或历史数据中的大小写差异导致文章详情漏掉相关发射；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-187`。
- DONE：来源默认实体引用已增加跨目录校验。`default_tags` 现在必须能匹配当前专题 slug，`default_companies` 必须能匹配当前公司 slug、中文名或英文名；daily catalog sync 会在写库和采集关系落库前阻止无效默认关系，避免新增来源配置通过解析但采集文章无法挂上预期专题或公司；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-186`。
- DONE：发射详情外部 ID 查询已改为大小写不敏感。`getLaunchByIdOrExternalId` 继续严格区分正整数内部 id 和外部 id，同时外部 id 分支会把路径值 trim 后统一小写，并用 `LOWER(external_id)` 查询，避免用户手输大小写混合的 Launch Library 外部 ID URL 时误触发 404；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-185`。
- DONE：公司和专题详情 slug 查询已改为大小写不敏感。`getCompanyBySlug` 与 `getTopicBySlug` 现在会把路径 slug trim 后统一小写，并用 `LOWER(slug)` 查询，避免用户手输 `/companies/Rocket-Lab` 或 `/topics/Reusable-Rockets` 这类大小写混合公开 URL 时误触发 404；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-184`。
- DONE：文章分类公开筛选别名已归一化。`/api/articles` 现在会把 `category=政策监管/政策/官方机构/公告信息/Policy` 归一为内部 `policy` 筛选，避免公开页面显示中文分类后，用户或外部调用按中文分类筛选时因为 DB 层只识别 `policy` 而退化成不过滤；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-183`。
- DONE：文章专题和公司 slug 筛选已改为大小写不敏感。`listArticles` 现在对 `tag` 和 `company` 的 slug 条件使用 `LOWER(...)` 比较，手工输入 `Reusable-Rockets`、`Rocket-Lab` 这类大小写混合 URL 不再漏掉小写存储的专题/公司关系，同时保留中文专题名、公司名和英文名筛选；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-182`。
- DONE：文章地区公开筛选值已归一化。`/api/articles` 现在会把 `region=国内/中国/CN` 映射为 `cn`，把 `region=国际/国外/International` 映射为 `global`，避免公开响应只展示中文 `regionLabel` 后，用户或外部调用按中文地区标签筛选却查不到对应文章；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-181`。
- DONE：启用来源公开展示名唯一性已前移到配置解析层。`parseSourcesConfig` 现在会在补齐公开分类默认值后校验 enabled 来源的公开展示名，拒绝 `RSSHub - 商业航天` 与 `Google News RSS - 商业航天` 这类清理后同名的配置，避免后续加源造成来源下拉重复和公开来源名到 source key 的筛选映射不确定；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-180`。
- DONE：发射状态公开展示中文别名已补齐。`displayLaunchStatus` 现在与公开筛选和数据库筛选保持一致，能把历史或手工数据中的“发射成功”“失败/异常”“等待”“待确认/确认”“评审”等中文状态显示为正确中文公开标签，避免发射列表、发射详情和首页状态区把中文存量状态误显示为“状态待定”；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-179`。
- DONE：发射状态数据库筛选中文别名已补齐。`listLaunches` 的 canonical 状态查询现在同时覆盖历史或手工数据中可能出现的中文状态文本，例如“发射成功”“失败/异常”“等待”“待确认/确认”，避免前端中文筛选值经 API 归一化后仍查不到中文存量状态；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-178`。
- DONE：发射状态数据库筛选子串误匹配已收紧。`listLaunches` 不再对所有状态统一使用 `LIKE '%状态%'`，`go/success/fail/hold/confirm/review` 等公共状态会生成更具体的查询条件，避免“准备发射”筛选查出 `No Go`/`ongoing`，或“发射成功”筛选查出 `Unsuccessful`；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-177`。
- DONE：中文发射状态“不成功”误匹配已修正。发射状态显示与筛选现在会把“不成功”归为“发射异常”，避免中文输入或上游中文状态因为包含“成功”被误显示或筛选为“发射成功”；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-176`。
- DONE：发射状态 `success` 子串误匹配已修正。发射状态显示与筛选现在会优先识别 `fail/unsuccess`，避免 `Unsuccessful` 这类上游状态因为包含 `success` 被误显示或筛选为“发射成功”；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-175`。
- DONE：发射状态 `go` 子串误匹配已修正。发射状态显示与筛选现在只把独立词 `Go`/`Launch Go` 识别为“准备发射”，`No Go` 会归为“等待窗口”，`ongoing` 不再因为包含字母组合 `go` 被误标为准备发射，避免上游状态脏值或人工筛选造成错误公共状态；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-174`。
- DONE：公开 URL 归一化规则已收敛为共享 helper。文章公开 API、发射公开 API 和前端外链安全过滤现在复用 `normalizeHttpUrl`，统一处理 trim、空值、相对 URL base 解析和 `http/https` 协议限制，避免后续公开 URL 边界规则在 API 与 UI 之间漂移；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check、重复实现搜索和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-173`。
- DONE：发射来源 URL 协议边界已收紧。发射公开序列化现在只返回 `http/https` 来源链接，空白、不可解析或非网页协议链接统一返回 `null`，避免历史 Launch Library 缓存或异常写入把非网页 URL 带入公开发射 API；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-172`。
- DONE：文章公开 URL 协议边界已收紧。公开文章序列化现在只返回 `http/https` 原文链接，空白、不可解析或非网页协议链接统一返回 `null`，避免历史数据或异常写入把 `javascript:`、`data:` 等不应公开的 URL 带入 API payload；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-171`。
- DONE：未知文章地区不再误标为国际。文章地区展示 helper 现在只把明确国内值显示为“国内”、明确国际值显示为“国际”，未知或空白地区显示“地区待确认”；文章卡片对该兜底标签不再生成 `region=global` 筛选链接，避免历史数据或异常采集记录被误归入国际商业航天；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-170`。
- DONE：发射任务公开名称空白兜底已补齐。发射公开序列化现在会在 `mission` trim 后为空时显示“发射任务 #id”，避免历史 Launch Library 缓存或异常写入让发射列表、发射详情和首页状态区出现空白发射标题；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-169`。
- DONE：公司和专题公开名称空白兜底已补齐。公司与专题公开序列化现在会在 `name` trim 后为空时分别显示“公司名称待确认”和“专题名称待确认”，避免历史数据或手工数据让公司目录、专题目录和详情页出现空白标题或空白列表项；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-168`。
- DONE：文章公开标题和摘要空白兜底已补齐。公开文章序列化现在会在标题或摘要 trim 后为空时分别显示“标题待确认”和“摘要待确认”，避免历史数据或异常采集记录在资讯卡片、列表和详情页产生空白标题、空白摘要或无意义的整卡可访问标签；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-167`。
- DONE：文章嵌套公司和专题引用已归一化。公开文章响应现在会 trim 嵌套 `tags/companies` 的 `slug/name`，空白名称使用 slug 兜底，空白 slug 的无效引用直接剔除，避免历史关系数据在文章卡片和详情页产生空白实体链接；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-166`。
- DONE：文章详情嵌套发射引用已归一化。文章详情公开响应现在会 trim 嵌套 `launches` 的 `externalId/missionName/name`，并在任务名空白时兜底为“发射任务 #id”，避免历史关联数据在文章详情页产生空白发射标签或不稳定链接文案；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-165`。
- DONE：文章相关来源空白项已从公开响应中剔除。`publicRelatedSources` 现在会在清理聚合器前缀后丢弃空白来源项，再做去重和重新计数，避免历史聚合数据把无实际来源名的“来源”计入多源覆盖或显示到详情页；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-164`。
- DONE：来源访问说明和公开徽标展示已归一化。`sourceDisplayMetadata` 现在会裁剪 `access_note/public_badge`，并把空白值统一归为 `null`，避免来源选项、来源目录或 API payload 出现空白括号、空白徽标或不稳定空格；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-163`。
- DONE：非聚合来源公开名称已统一 trim。`sourceDisplayName` 现在不仅清理 Google News/RSSHub 等聚合器前缀，也会裁剪普通来源公开名称并为空白来源名提供“来源”兜底，避免历史配置或数据库中的前后空格进入来源目录、文章来源和筛选选项；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-162`。
- DONE：无效文章时间和发射窗口显示已中文兜底。文章时间不可解析时不再原样显示脏字符串，而是显示“时间待定”；发射窗口不可解析时显示“窗口待定”，避免历史数据或上游异常时间污染资讯卡片、发射列表和发射详情页；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-161`。
- DONE：未知发射状态公开兜底已中文化。发射公开响应不再把 Launch Library 2 的未知状态原文直接显示给页面，`TBD/TBC` 等待定状态会映射为“待确认”，其他未知或空白状态统一显示为“状态待定”；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-160`。
- DONE：未知公司国家和赛道公开兜底已中文化。即使历史数据库或手工数据残留未映射的公司 `country/sector`，公开 API 也不再把英文原始值直接返回给页面，而是显示“地区待确认”或“赛道待确认”，避免公司目录和详情页重新出现内部/英文分类值；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-159`。
- DONE：未知专题分类公开兜底已中文化。即使历史数据库或手工数据残留未映射的专题 `category`，公开 API 也不再把原始分类值直接返回给页面，而是统一显示为“专题”，避免内部分类或脏数据进入中文站点界面；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-158`。
- DONE：公司国家和赛道配置已收紧为公开标签支持的分类表。公司 `country` 现在只能使用已映射国家，`sector` 的逗号分隔项也会逐项校验，并由共享公司分类表同时驱动配置校验和公开 API 中文标签；同时补齐 `satellite internet` 小写赛道别名，避免 SpaceX 等公司页面显示半中文半英文赛道。目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-157`。
- DONE：专题分类配置已收紧为固定枚举。专题 `category` 现在只能使用公开 API 支持中文标签映射的 `technology`、`market`、`launch`、`company`、`policy` 分类，并由共享分类表同时驱动配置校验和公开标签映射，避免后续新增专题时拼错分类导致页面显示内部原始值或分类漂移；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-156`。
- DONE：专题配置关键词已改为必须非空。`parseTopicsConfig` 现在会在 trim、去空白、去重后要求每个专题至少保留一个关键词，避免新增专题只有目录入口但无法参与自动标签匹配和内容归集；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-155`。
- DONE：人工精选权重范围已收紧。`home_highlights`、`pinned_items` 和专题精选的 `weight` 现在限制为 0-100 整数，避免配置误填极大值或负值长期劫持首页/专题排序；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-154`。
- DONE：来源配置合规风险说明已改为必填。`risk_notes` 现在会在来源 schema 解析阶段 trim 后要求非空，避免后续新增来源时绕过“必须记录失败行为/合规风险”的项目约束；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-153`。
- DONE：API 类采集器上游 `limit` 参数已统一归一化。SNAPI 和 Launch Library 2 不再透传来源 URL 中的异常 `limit` 字符串，统一按来源 `max_items`、十进制 safe integer、默认 25 和上限 100 生成上游请求数量，避免新增来源配置把 `1e3`、`0` 或超大值传给外部 API；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-152`。
- DONE：发射详情数字 id 判定已复用严格正整数规则。`getLaunchByIdOrExternalId` 不再用 `Number()` 判断内部 id，避免 `/launches/1e3` 这类非十进制值被映射到内部 id `1000`；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-151`。
- DONE：前端正整数 URL 归一化已对齐后端边界。`normalizePositiveInteger` 现在只接受 trim 后的十进制数字串或 safe integer 数值，分页解析和分页链接生成会拒绝 `1e3`、`+12`、超大整数等非规范参数，避免前端页码状态与 API 参数语义漂移；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-150`。
- DONE：公开 API 正整数解析边界继续收紧。共享 `parseOptionalPositiveInteger` 只接受 trim 后的十进制数字串和 safe integer，文章详情路由复用该 helper 拒绝 `1e3`、`+12`、超大整数等非规范 id，避免异常参数被静默映射到其他文章或分页值；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-149`。
- DONE：文章公开响应空白字段与相关来源计数已归一化。文章公开序列化现在会 trim 标题、摘要、URL 和发布时间，并把空白原文标题、原文摘要、publisher 归一为 `null`；同时在清理聚合器前缀后按唯一公开来源重新计算 `relatedSourceCount`，避免页面显示“多源覆盖”数量与相关来源列表不一致；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-148`。
- DONE：发射公开响应空白字段已归一化。发射公开序列化现在会 trim `externalId/mission/status`，并把空白 `rocket/provider/windowStart/site/sourceUrl` 归一为 `null`，避免历史发射缓存让发射列表或详情页显示空白发射商、火箭型号、场站或外链字段；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-147`。
- DONE：专题公开响应空白字段已归一化。专题公开序列化现在会 trim `slug/name/category/itemUrl`，并把空白精选备注归一为 `null`，避免历史数据或手工数据让专题列表、详情和精选资料 payload 保留不稳定空白；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-146`。
- DONE：公司公开响应空白字段已归一化。公司公开序列化现在会 trim `slug/name/profile/country/sector`，并把空白 `englishName/website/stockSymbol/logoUrl` 归一为 `null`，避免历史数据或手工数据让公司详情显示空简介、空股票代码或空外链字段；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-145`。
- DONE：来源筛选已兼容公开名清理前的旧显示值。文章 API 的来源筛选映射现在同时接受当前公开来源名、历史原始配置名和 source key，避免 `RSSHub - ...` 等展示名清理后旧链接或旧选项值筛不到文章；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-144`。
- DONE：文章卡片整卡跳转行为已补交互回归保护。整卡点击/键盘进入详情的激活条件已抽为可测试 helper，并覆盖普通主键点击、修饰键/中键/已阻止点击、嵌套交互元素点击和 Enter 键，避免后续改动再次造成“点击新闻没有反应”或破坏原文/标签链接原生行为；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-143`。
- DONE：前端文章来源展示已清理 publisher 聚合器前缀。`articlePublisherLabel` 现在会对 `publisherName` 和 `sourceName` 使用同一聚合器前缀清理逻辑，避免旧缓存或异常 payload 在文章卡片和详情页显示 `Google News RSS -`、`RSSHub -` 这类实现名；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-142`。
- DONE：来源目录未知配置 fallback 已清理聚合器前缀。`/api/sources` 在数据库有启用来源但配置文件找不到对应 key 时，不再直接返回原始 `source.name`，而是复用共享展示名清理逻辑，避免配置漂移或历史数据让来源目录显示 `RSSHub -` 这类实现名；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-141`。
- DONE：RSSHub 来源公开展示名已清理实现前缀。共享来源展示 helper 现在会移除 `RSSHub -` 前缀，来源目录、health、来源筛选和文章公开序列化复用同一公开名称逻辑，避免后续启用 RSSHub 来源时页面显示聚合器实现名；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-140`。
- DONE：文章公开响应来源名已改用公开展示名。文章 API 不再直接返回数据库中的原始 `sourceName`，而是优先使用来源配置的公开名称，并清理 publisher 与相关来源里的聚合器前缀，避免历史数据继续在页面或 API 中显示 `Google News RSS` 这类实现名；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和敏感信息模式扫描已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-139`。
- DONE：专题精选资料公开响应已移除数据库 id。专题详情页不再依赖精选资料 `id` 作为渲染 key，公开精选资料 payload 只保留 URL、备注和创建时间，避免继续暴露内部自增 id；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-138`。
- DONE：公司和专题公开响应已移除未使用的数据库 id。公司与专题页面使用 `slug` 路由，公开公司/专题列表和详情不再返回自增 `id`，前端类型与相关 fixture 已同步收敛；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-137`。
- DONE：文章公开响应已移除原始语言和地区码。公开文章序列化不再返回 `language: en/zh` 和 `region: cn/global`，改为返回中文 `regionLabel`，文章详情和列表映射直接消费公开地区标签；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-136`。
- DONE：发射公开响应已移除原始状态值。`/api/launches` 和 `/api/launches/:id` 不再返回 `status: Go/TBD` 这类上游状态原文，公开响应改为 `statusLabel` 中文状态，发射列表、详情和首页 HUD 直接消费公开标签；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-135`。
- DONE：公司公开响应已移除原始国家和赛道字段。`/api/companies` 和 `/api/companies/:slug` 不再返回 `country: United States`、`sector: Launch` 这类英文原始字段，前端类型与测试 fixture 改为只消费中文 `countryLabel/sectorLabel`；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-134`。
- DONE：专题公开响应已移除原始分类枚举。`/api/topics` 和 `/api/topics/:slug` 不再返回 `category: technology/market/policy` 这类内部分类码，前端类型与测试 fixture 改为只消费中文 `categoryLabel`；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-133`。
- DONE：公司和专题中文标签类型契约已收紧。公司 `countryLabel/sectorLabel` 与专题 `categoryLabel` 在公开 API 中总是返回，前端类型已改为必填并移除页面内对原始 `country/sector/category` 的显示兜底，避免标签遗漏时静默退回英文/内部分类；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-132`。
- DONE：来源统计前端类型契约已收紧。`ApiSourceListResult` 现在把 `/api/sources` 总是返回的 `publicStats` 和 `accessStats` 标为必填，并修正相关测试 fixture，避免未来来源状态统计字段被测试或前端代码静默遗漏；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-131`。
- DONE：来源公开响应已移除原始区域码。`/api/sources` 不再返回 `region: cn/global` 这类内部区域码，来源列表公开契约只保留展示所需的来源名、中文分类、国内/全球访问标签、访问说明和徽标；目标测试、配置检查、类型检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-130`。
- DONE：文章公开响应已移除来源分类英文枚举。公开文章序列化不再返回 `sourceCategory` 原始枚举，前端文章分类改用中文 `sourceCategoryLabel` 判断政策/商业航天分类，并补充结构化字段断言避免 `sourceCategoryLabel` 被误判为内部字段泄露；目标测试、公开字段搜索、配置检查、类型检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-129`。
- DONE：来源分类公开响应已移除英文枚举。`/api/sources` 不再返回 `publicCategory: media/official/notice` 或 `publicStats.category`，前端来源筛选改用中文 `categoryLabel`，避免来源 API 和筛选逻辑继续暴露内部分类枚举；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-128`。
- DONE：来源访问状态公开响应已中文化。`/api/sources` 不再把 `direct/limited/blocked/unknown`、`directCount/limitedCount` 等内部状态枚举和计数字段作为公开契约返回，改为 `domesticAccessLabel`、`globalAccessLabel`、`accessSummaryLabel` 和访问统计中文标签，前端来源状态直接消费公开标签；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-127`。
- DONE：采集超时回归测试已降低墙钟抖动。定时采集超时测试的健康来源分支不再被 10ms 预算误伤，避免全量回归在慢一次事件循环时误报失败；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-126`。
- DONE：来源筛选公开契约已移除内部 source key。`/api/sources` 不再返回 `snapi`、`cnsa-news` 等内部来源标识，前端来源下拉改用公开来源名称作为筛选值，文章 API 会把公开名称映射回内部 key 并继续兼容旧 key URL；同时补充 enabled 来源公开名称唯一性和禁用来源不参与映射的回归保护；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-125`。
- DONE：固定来源 admin 手动采集已尊重启用状态。SNAPI 和 Launch Library 2 手动入口现在只会运行 enabled 来源，避免配置禁用后仍能通过受保护管理端触发；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-124`。
- DONE：admin RSS 手动采集入口已覆盖 RSSHub。`/api/admin/ingest/rss` 现在会包含启用的 `rss` 和 `rsshub` 来源，并注册对应采集器，避免定时采集与手动采集覆盖范围漂移；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-123`。
- DONE：Google News RSS 请求标识已补齐。备用聚合源请求现在带明确 `SpaceIntelBot` User-Agent，与其他采集器的上游访问标识保持一致，便于未来人工启用后的访问排查；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-122`。
- DONE：SNAPI 请求标识已补齐。核心新闻 API 采集请求现在带明确 `space-intel` User-Agent，与 RSS 和 Launch Library 2 采集器的上游访问标识保持一致；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-121`。
- DONE：RSSHub 来源调度入口已补齐。`rsshub` 类型会复用 RSS 元数据采集器，并在 hourly scheduled ingestion 中按启用状态运行，避免未来启用 RSSHub 来源后被静默跳过；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-120`。
- DONE：API 采集器数量控制已对齐。SNAPI 和 Launch Library 2 会优先使用来源级 `max_items` 作为上游请求和最终输出上限，避免 API 类来源与 RSS/页面类来源的数量控制语义漂移；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-119`。
- DONE：Google News RSS 数量控制已补齐。备用聚合源会在过滤无效链接后应用 `max_items`，并保留默认输出上限；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-118`。
- DONE：RSS `max_items` 计数语义已修正。无效 feed 链接不再占用来源级采集数量预算，`max_items` 限制最终有效记录数；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-117`。
- DONE：API 采集器上游 URL 协议边界已收紧。SNAPI 文章链接和 Launch Library 来源链接已复用 `http/https` 归一化，避免非网页 URL 进入文章或发射记录；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-116`。
- DONE：公司目录公开 URL 协议边界已收紧。company `website` 和 `logo_url` 只接受 `http/https`，同时保留空白值按未配置处理；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-115`。
- DONE：人工精选配置 URL 协议边界已收紧。home、pinned 和 topic curated links 只接受 `http/https`，避免非网页链接进入精选配置；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-114`。
- DONE：来源配置 URL 协议边界已收紧。`config/sources.yaml` 的来源地址只接受 `http/https`，避免后续新增来源时把非网页采集地址写入配置；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-113`。
- DONE：RSS 类采集链接协议边界已收紧。标准 RSS、Google News RSS 和 HTML 列表采集已复用统一 `http/https` URL 归一化，避免非网页链接进入文章记录；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-112`。
- DONE：HTML 列表采集链接协议边界已收紧。官方页和采购页共享的列表链接抽取现在只接受 `http/https`，避免非网页 href 进入采集数据；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-111`。
- DONE：来源展示元数据空白值归一化已完成。`access_note` 和 `public_badge` 空白编辑值会按未配置处理，避免后续新增来源时产生空徽标或空说明；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-110`。
- DONE：专题精选资料无备注时的显示名已优化。页面不再把完整 URL 当标题展示，保留外链但用域名作为兜底标签；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-109`。
- DONE：首页实时侧栏可访问名称已中文化。`aria-label` 不再使用 `HUD` 这类英文界面缩写；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-108`。
- DONE：未来发射接近度文案已修正。超过一天后的发射不再显示 `T+N`，避免把待发射任务读成已发生后的时间；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-107`。
- DONE：顶部命令菜单已补齐关键词搜索行为。命令弹窗输入关键词后可以进入 `/articles?query=...`，不再只是频道/公司/专题导航；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check、code secret-pattern scan 和本地 Playwright 命令菜单跳转验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-106`。
- DONE：发射列表 hasMore 说明文案已移除。筛选控件保留，但不再常驻显示“可用关键词、发射商或状态继续筛选”这类操作说明；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-105`。
- DONE：文章高级筛选的专题/公司建议值已从内部 slug 改为公开名称，避免选择建议后输入框出现 `reusable-rockets`、`rocket-lab` 这类标识符；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-104`。
- DONE：文章专题/公司筛选已补齐公开显示名匹配。保持现有 slug URL 语义，同时让手动输入 `可回收火箭`、`Rocket Lab` 这类可见名称也能命中；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-103`。
- DONE：发射商筛选已改为大小写不敏感关键词匹配。避免自由文本输入必须精确匹配完整 DB 值；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-102`。
- DONE：发射状态筛选已对齐中文显示文案。`/api/launches` 会把“准备发射/等待窗口/发射成功”等公开状态归一到上游状态关键词并执行大小写不敏感匹配；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-101`。
- DONE：顶部搜索框可见快捷键提示已移除。保留命令面板快捷键行为但不再在页面显示 `Ctrl K`；目标测试、类型检查、可见文案搜索、配置检查、lint、完整回归、构建、diff check、code secret-pattern scan 和 desktop/tablet/mobile 布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-100`。
- DONE：发射详情元数据展示已补齐。详情页现在显示中文状态、发射商、火箭型号和发射场，并保留待定/未披露兜底；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check、code secret-pattern scan 和 desktop/tablet/mobile 布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-099`。
- DONE：公司国家和赛道展示已增加中文公开标签。company list/detail API 返回 `countryLabel/sectorLabel`，公司列表和详情优先显示中文标签并保留旧字段兼容；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-098`。
- DONE：专题分类展示已增加中文公开标签。`/api/topics` 返回 `categoryLabel`，专题列表优先显示中文标签并保留旧字段兼容；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-097`。
- DONE：首页来源统计配置覆盖规则已修复。`/api/home` 的来源分类统计现在按 source key 使用配置分类聚合，未知来源继续按 collector type 兜底；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-096`。
- DONE：文章来源分类配置覆盖规则已修复。公开文章序列化现在优先使用来源配置里的 `public_category`，未知来源继续按 collector type 兜底；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check 和 code secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-095`。
- DONE：发射列表前端类型契约已补齐。`ApiLaunchListResult` 已补充公开 API 实际返回的 `page` 和 `limit`，并更新发射列表/HUD 测试 fixture；目标测试、类型检查、fixture 搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-094`。
- DONE：文章来源分类公开契约已收敛。公开文章 payload 已从 collector `sourceType` 改为用户语义 `sourceCategory/sourceCategoryLabel`，前端文章展示分类和来源兜底同步改用公开字段；目标测试、类型检查、公开路由/前端字段搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-093`。
- DONE：专题详情精选资料响应边界已收敛。专题详情 API 已增加公共序列化，只保留精选资料的 `id`、`itemUrl`、`note`、`createdAt`，不再公开 `targetType`、`targetKey`、`enabled` 等内部路由字段；目标测试、类型检查、公开路由/前端字段搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-092`。
- DONE：发射 API 来源链接字段已收敛。发射列表和详情公开响应已从内部 `rawUrl` 字段改为用户语义 `sourceUrl`，前端发射详情页同步改用新字段；目标测试、类型检查、公开路由/前端字段搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-091`。
- DONE：来源目录公开响应边界已收敛。`/api/sources` 已移除公开响应中的 collector `type`、`credibility` 和内部 type `stats`，前端来源筛选改为使用公开来源分类；目标测试、类型检查、公开字段搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-090`。
- DONE：首页来源统计响应边界已收敛。`/api/home` 已把内部 `enabledSourcesByType` 公开字段改为用户语义 `enabledSourceCategories`，`LiveHud` 使用中文来源分类作为 sources API 未返回时的兜底；目标测试、类型检查、公开字段搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-089`。
- DONE：公开文章 API 响应边界已收敛。首页、文章列表/详情、公司详情和专题详情已接入公共文章序列化，不再返回 `sourceKey`、`fetchStatus`、`translationStatus`、`translationProvider` 和 `storyKey` 等内部工作流字段；目标测试、类型检查、公开路由字段搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-088`。
- DONE：admin 未授权响应文案已中文化。`requireAdminRequest` 已从英文 `Unauthorized` 改为共享中文 `未授权。`，保持 401 状态不变；目标测试、类型检查、旧英文文案搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-087`。
- DONE：admin 缺失采集来源响应已收敛。SNAPI 和 Launch Library 手动采集入口在配置来源缺失时已改为共享中文响应 `采集来源未配置。`，避免响应体暴露采集器实现名；目标测试、类型检查、旧英文错误文案搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-086`。
- DONE：health 检查字段实现词已清理。`/api/health` 已从 `bindings.d1/r2` 改为通用 `checks.database/assets`，避免公开响应暴露 Cloudflare 绑定命名；目标测试、类型检查、health 字段搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-085`。
- DONE：应用级错误兜底可访问性已优化。`ErrorBoundary` 的兜底提示已改为带 `role="alert"` 的统一 fallback 组件，便于渲染失败时被辅助技术感知；目标测试、类型检查、局部 lint、配置检查、完整 lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-084`。
- DONE：外链新标签页隔离属性已补齐。所有 `target="_blank"` 外链已统一为 `rel="noopener noreferrer"`，避免未来维护时弱化 opener 隔离语义；目标测试、类型检查、旧 rel 写法搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-083`。
- DONE：health 诊断来源标识已优化。`/api/health` 已从公开响应中的 raw `sourceKey` 改为用户语义来源名称，未知来源兜底为“来源”；目标测试、类型检查、配置检查、lint、完整回归、构建、diff check、health 响应 key 搜索和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-082`。
- DONE：admin 维护入口失败响应已收敛。catalog sync、curation sync、entity enrichment 和 translation backfill 已改为本地捕获异常、服务端记录原始错误并返回稳定中文失败响应；目标测试、类型检查、admin catch 覆盖搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-081`。
- DONE：admin 采集错误响应已收敛。RSS、Google News RSS、Launch Library 和 SNAPI 手动采集入口已改为服务端记录原始异常、响应体返回稳定中文失败文案，避免把 collector 原始错误直接暴露给调用方；目标测试、类型检查、raw error 搜索、配置检查、lint、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-080`。
- DONE：来源目录失败反馈延迟已优化。`useSourcesQuery` 已改为不等待默认 retry 周期，来源筛选和首页来源状态在 `/api/sources` 失败时可以更快进入“暂不可用”反馈；目标测试、类型检查、配置检查、完整回归、构建、diff check 和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-079`。
- DONE：外链协议兜底校验已补齐。文章原文、公司官网、专题精选资料和发射来源现在只渲染 `http/https` URL，`javascript:`、`data:` 或 malformed URL 会被隐藏；目标测试、类型检查、配置检查、完整回归、构建、直接 href 搜索、secret-pattern scan 和布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-078`。
- DONE：前端详情路由编码已收敛。文章、公司、专题和发射详情链接现在复用统一 route helper 编码动态路径段，避免外部标识含空格、斜杠或查询字符时生成错误路由；目标测试、类型检查、配置检查、完整回归、构建、动态详情链接搜索和 secret-pattern scan 已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-077`。
- DONE：来源筛选选项状态已补齐。`SourceOptions` 在来源目录加载、失败或筛选后为空时会显示禁用状态选项，资讯/政策筛选框不再只剩“全部来源”而没有状态反馈；组件测试、类型检查、配置检查、完整回归、构建和 desktop/mobile 筛选控件布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-076`。
- DONE：详情页初始加载态已补齐。文章、公司、专题和发射详情页在请求未返回且没有详情数据时会展示统一详情骨架，不再显示近似空壳页面；页面测试、类型检查、配置检查、完整回归、构建和 desktop/mobile 加载态布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-075`。
- DONE：公司库和专题追踪加载态已补齐。两个索引页在初次加载且暂无数据时会展示实体卡片骨架，避免空网格被误解为空内容；页面测试、类型检查、配置检查、完整回归、构建和 desktop/mobile 加载态布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-074`。
- DONE：前端文章列表 limit 上限已对齐。文章与政策页现在会把超大正整数 `limit` 限制到后端 50 条上限，页面状态、API URL 和分页链接不再暗示超过后端契约的页大小；目标测试、类型检查、配置检查、完整回归和构建已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-073`。
- DONE：首页 Live HUD fallback 发射链接已修复。`LiveHud` 现在与 `LaunchesPage` 一致，把 `isFallback` 发射记录作为静态条目处理，避免链接到可能不存在的详情页；组件测试、类型检查、配置检查、完整回归、构建和 desktop/tablet/mobile 布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-072`。
- DONE：首页 Live HUD 加载态已修复。发射、政策和来源状态区块在初次请求未返回时不再显示“暂无/暂不可用”类结果态，加载、错误和成功空结果已拆分；组件测试、类型检查、配置检查、完整回归、构建和 desktop/tablet/mobile 布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-071`。
- DONE：发射列表加载态已补齐。`LaunchesPage` 初次加载且没有数据时会展示发射时间线骨架，避免空白区域被误解为空结果；页面测试、类型检查、配置检查、完整回归、构建和 desktop/tablet/mobile 布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-070`。
- DONE：发射详情错误态已优化。接口错误且没有发射记录时不再展示“发射窗口：记录暂时不可访问”等占位字段，只保留错误提示和返回入口；页面测试、类型检查、配置检查、完整回归、构建和 desktop/tablet/mobile 布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-069`。
- DONE：前端 not-found 错误判断已收紧。`isNotFoundError` 现在只匹配独立 `404` token，避免 `HTTP 1404` 或 `4040` 这类非 404 文本误用记录不存在文案；目标测试、类型检查、配置检查、完整回归和构建已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-068`。
- DONE：详情页 not-found 错误映射已收敛。专题详情和发射详情复用扩展后的 `friendlyError`，保留当前用户可见文案但移除页面内重复 `isNotFoundError` 分支；目标测试、类型检查、配置检查、完整回归和构建已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-067`。
- DONE：bounded positive integer 归一化已收敛。首页查询、保留清理和翻译限制值复用共享 helper，避免继续分散 `Math.floor` 小数截断逻辑；目标测试、类型检查、配置检查、完整回归和构建已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-066`。
- DONE：DB 查询层分页归一化已收紧。文章和发射查询与公开 API/前端保持一致，拒绝小数 page/limit 并回退默认值；查询层测试、类型检查、配置检查、完整回归和构建已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-065`。
- DONE：文章详情错误态已优化。文章详情页会区分 404 类记录不可访问错误，并避免接口错误时渲染“来源暂不可用/时间暂不可用”等占位元数据；页面测试、类型检查、配置检查、完整回归、构建和 desktop/tablet/mobile 布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-064`。
- DONE：列表页空状态判断已收敛。资讯、政策、发射、公司、专题和首页列表区域会避免在请求错误态同时显示“暂无记录/没有匹配结果”文案；页面测试、类型检查、配置检查、完整回归、构建和 desktop/tablet/mobile 布局验证已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-063`。
- DONE：公司详情错误态已优化。公司详情页会区分 404 类记录不可访问错误，并避免错误态同时显示“暂无相关新闻”的空列表文案；页面级测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-062`。
- DONE：D1 prepared statement batch/fallback 执行逻辑已收敛。`catalog`、`curations`、`launches`、`entityLinks` 和 `articles` 等 DB persistence 模块复用共享 helper，避免后续 D1 batch 兼容规则分散维护；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-061`。
- DONE：前端 404 错误判断已收敛。`friendlyError`、专题详情和发射详情共用 `isNotFoundError`，避免分散匹配 `404` 字符串；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-060`。
- DONE：发射页错误文案重复计算已收敛。`LaunchesPage` 只计算一次 `friendlyError` 结果，再用于条件渲染和展示；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-059`。
- DONE：页面查询参数 trim 逻辑复用已收敛。`PolicyPage` 和 `LaunchesPage` 复用 `trimmedSearchParams`，避免 URL 清理规则继续分散；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-058`。
- DONE：分页链接无效 `limit` 参数清理已完成。文章与政策分页链接只保留真正的正整数 limit，避免 URL 显示值与实际请求默认值不一致；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-057`。
- DONE：前端分页链接参数清理已完成。文章与政策分页链接复用 trim/drop 规则，不再携带空白筛选项或未知 query 参数；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-056`。
- DONE：前端正整数 URL 参数解析已收紧。前端 `parsePositiveInteger` 与后端一致，拒绝 `3.9` 这类小数并回退默认值；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-055`。
- DONE：curations 仓库配置测试已放宽为结构性验证。测试确认真实 YAML 可解析、记录类型合法，不再锁死当前人工精选必须为空；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-054`。
- DONE：真实 catalog/curations 配置解析回归已补齐。company/topic/curations parser 测试覆盖仓库实际 YAML，避免配置规则收紧后只靠间接检查发现问题；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-053`。
- DONE：source 可选列表空白项处理已统一。`default_tags`、`default_companies`、`include_terms`、`exclude_terms` 允许显式空字符串进入归一化流程，并按空白项丢弃或转为未配置处理；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-052`。
- DONE：route-safe 标识符校验复用已完成。source key、catalog slug 和 curation target key 共用 `routeSafeIdentifierSchema`，避免后续格式规则漂移；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-051`。
- DONE：人工精选 target key 格式校验已完成。curations 的 `pinned_items.target` 与 topic curation `slug` 限制为小写字母、数字和单个连字符分隔，避免未来人工精选配置产生不一致 targetKey；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-050`。
- DONE：配置标识符格式校验已完成。source `key` 与 company/topic `slug` 限制为小写字母、数字和单个连字符分隔，避免未来新增来源或 catalog 项时产生 route-unsafe 标识；当前 sources/companies/topics 配置扫描无违规，相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-049`。
- DONE：catalog 可选 URL 空白值处理已完成。公司 `website` / `logo_url` 的空白编辑值会按未填写处理，而非触发 URL 校验失败；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-048`。
- DONE：人工精选配置字段归一化已完成。curations 的 target、topic slug、URL 和 note 会在解析阶段 trim，确保重复精选目标校验基于规范值执行；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-047`。
- DONE：source 标识字段归一化已完成。来源 key/name/purpose 等关键字段会在 schema 解析阶段 trim，确保重复 source key 校验基于规范值执行；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-046`。
- DONE：catalog 标识字段归一化已完成。公司和专题 slug/name/category 等关键字段会在 schema 解析阶段 trim，确保重复 slug 校验基于规范值执行；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-045`。
- DONE：专题关键词配置规范化已完成。`parseTopicsConfig` 会 trim/dedupe `keywords` 并丢弃空白项，避免实体匹配层继续接收脏词表；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-044`。
- DONE：空白来源列表默认值语义修复已完成。来源列表归一化后如果只剩空白项，现在按未配置处理，避免屏蔽采集器默认 include/exclude 或默认标签；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-043`。
- DONE：admin endpoint 环境类型收敛已完成。鉴权 helper 集中后，重复的 `ADMIN_TOKEN` 环境字段声明已收敛到共享 `AdminEnv` 类型，降低后续鉴权字段调整的维护成本；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-042`。
- DONE：来源过滤词表规范化已完成。`include_terms` 和 `exclude_terms` 会在配置解析阶段 trim/dedupe 并丢弃空白项，避免来源相关性过滤被空格或重复词干扰；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-041`。
- DONE：公开 API 正整数参数解析收紧已完成。共享 `parseOptionalPositiveInteger` 现在只接受真正的正整数，不再把 `3.9` 截断为 `3`，避免错误 query 参数被静默接受；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-040`。
- DONE：采购页采集数量配置化已完成。`procurement_page` 采集器现在尊重 `source.max_items`，与 RSS 和官方网页源的来源级数量控制保持一致；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-039`。
- DONE：来源默认关系元数据规范化已完成。`parseSourcesConfig` 现在会 trim/dedupe `default_tags` 和 `default_companies`，减少采集记录携带重复或空白默认关系的机会；相关测试、类型检查、配置检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-038`。
- DONE：来源默认元数据映射收敛已完成。配置解析和展示层现在共用 `sourceMetadata` 中的公共分类与国内访问默认规则，避免新增来源类型时两边漂移；相关测试、类型检查、重复映射搜索和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-037`。
- DONE：聚合来源前缀清洗复用已完成。`sourceDisplayName` 和 `articlePublisherLabel` 现在共用 `stripAggregatorPrefix`，避免 Google News 类展示规则重复维护；相关测试、类型检查、重复实现搜索和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-036`。
- DONE：来源统计死代码清理已完成。`/api/sources` 改为单次读取后，已移除未使用的 `listEnabledSourceTypeStats` 和 `SourceTypeStatRow`，减少误用旧统计查询的维护风险；相关测试、类型检查、引用搜索和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-035`。
- DONE：来源 API 统计读优化已完成。`/api/sources` 现在只读取一次启用来源列表，并从同一结果派生 type stats，减少一次 D1 查询；相关测试、类型检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-034`。
- DONE：来源公共分类和访问状态默认值前移已完成。`parseSourcesConfig` 现在为缺省来源补齐公共分类、国内访问状态和全球访问状态，减少后续加源漏标风险；相关测试、类型检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-033`。
- DONE：实体关联重复 slug 去重已完成。`upsertConfiguredEntityLinks` 在构造 relation batch 前对单篇文章的公司和专题 slug 去重，避免重复 `INSERT OR IGNORE` no-op 语句；目标测试、类型检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-032`。
- DONE：实体关联回填 batch 优化已完成。`upsertConfiguredEntityLinks` 将公司和专题关联写入合并为一次 D1 batch，并保留顺序 fallback；目标测试、类型检查和完整回归已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-031`。
- DONE：布局验证脚本政策化已完成。`verify-layout` 不再检查旧“资本/投资建议”文案，改为验证首页政策动态信号和政策页导航，避免回归脚本与当前产品方向不一致。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-030`。
- DONE：配置生成脚本并行化已完成。`generate-config` 现在并行处理独立 YAML/JSON 配置对，并保持输出顺序稳定；`--check` 语义不变。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-029`。
- DONE：catalog 配置同步 batch 优化已完成。sources、companies、topics 三类 upsert 保持原有类型顺序，但每类内部改为批量 statement 执行，减少 D1 配置同步写入往返；已补充 catalog persistence 回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-028`。
- DONE：人工精选替换写入 batch 优化已完成。`replaceConfiguredCurations` 保持先删后插语义，但插入阶段改为一次批量 statement 执行；已补充 curations persistence 回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-027`。
- DONE：人工精选配置重复项校验已完成。curations 解析阶段会拒绝同一 target 下重复 URL，避免首页、置顶或专题精选出现重复记录；已补充 curations config 回归测试并通过 config check。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-026`。
- DONE：公司与专题配置 slug 唯一性校验已完成。配置解析阶段会拒绝重复 company/topic slug，避免后续 catalog upsert 覆盖或实体关系歧义；已补充 catalog config 回归测试并通过 config check。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-025`。
- DONE：发射缓存写入 batch 优化已完成。Launch Library 发射记录 upsert 从逐条 await 改为批量 statement 执行，D1 支持 batch 时一次提交整批发射缓存；已补充 launch ingestion batch 回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-024`。
- DONE：文章关系写入 batch 合并优化已完成。单篇文章的标签、公司和发射关系写入从三组独立执行收敛为一次关系 batch，减少 D1 写入往返；持久化回归测试已覆盖 batch 大小和关系结果。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-023`。
- DONE：公开详情 API 404 响应文案优化已完成。文章、公司、专题和发射详情不存在时统一使用中文公共错误响应，避免公开 API 兜底 payload 出现英文站外式文案。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-022`。
- DONE：来源状态兜底标签优化已完成。`LiveHud` 在 `/api/sources` 尚未返回时，不再用 `api`、`rss`、`google_news_rss` 等内部类型作为来源状态标签，而是映射为数据来源、专业媒体、来源等用户语义标签。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-021`。
- DONE：翻译回填 admin limit 参数解析优化已完成。`/api/admin/translate/backfill` 复用共享正整数解析，避免 `limit=0`、负数或非数字进入回填逻辑；本地重复 `parseLimit` 已移除。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-020`。
- DONE：admin endpoint 鉴权收敛已完成。所有 admin POST endpoint 改用共享 `ADMIN_TOKEN` Bearer 鉴权 helper，移除重复 token 判断，并收紧为只接受 `Authorization: Bearer ...` 形式；已补充 admin 鉴权回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-019`。
- DONE：详情查询路径参数规范化已完成。公司、专题和发射详情查询在数据库层裁剪 slug / external id 前后空格，避免编码空格路径误触发 404；已补充 company/topic/launch 查询回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-018`。
- DONE：前端筛选 URL 构造规范化已完成。资讯、政策和发射页面生成 API 查询路径时会裁剪过滤参数前后空格并丢弃空白值，减少重复请求键和异常空结果；已补充 URL helper 回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-017`。
- DONE：公开 API 文本过滤参数规范化已完成。`/api/articles` 与 `/api/launches` 的文本过滤参数统一 trim，空白参数转为未设置，避免带前后空格的来源、标签、公司、状态或发射商过滤误返回空结果。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-016`。
- DONE：公开 API 正整数参数解析优化已完成。新增共享请求解析工具，统一 `/api/home`、`/api/articles` 和 `/api/launches` 对 `page`/`limit` 的正整数处理；无效、0 或负数不再进入下游业务逻辑，首页 `limit` 会回退默认值。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-015`。
- DONE：来源状态 API 低风险性能优化已完成。`/api/sources` 的启用来源列表和来源类型统计改为并行等待，前台来源展示与访问标识返回结构不变并已通过 sources API 回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-014`。
- DONE：健康检查 API 低风险性能优化已完成。`/api/health` 的最新文章、开放采集日志、最近成功采集、最新采集日志和最近失败日志查询改为并行等待，诊断返回结构不变并已通过 health API 回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-013`。
- DONE：专题详情 API 低风险性能优化已完成。专题详情在确认专题存在后并行读取最近文章和人工精选，文章查询仍保留旧 schema 兼容兜底；返回结构不变并已更新专题查询回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-012`。
- DONE：首页 API 低风险性能优化已完成。`/api/home` 的文章、统计和趋势标签查询改为并行等待，首页统计内部的独立 D1 查询也改为并行等待，减少首屏 API 不必要的串行延迟；返回结构不变并已补充 `homeQueries` 回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-011`。
- DONE：新闻卡片点击体验修复已完成。首页、资讯、政策、公司详情和专题详情复用的新闻卡片现在整卡可进入文章详情，同时保留地区、公司、专题和原文链接的独立点击行为；本地生产站复现确认标题链接可跳转但卡片空白/摘要点击无反应，修复后已补充组件回归测试。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-010`。
- DONE：来源展示与访问标识升级已完成。前台来源展示不再暴露 `Google News RSS`、`google_news_rss`、`API 源`、`RSS 源`、`备用聚合` 等采集实现词；来源 API 增加官方机构、专业媒体、行业组织、公告信息、数据来源和来源等用户语义分类，以及国内/全球访问状态；Google News 类采集会保留原始发布方用于文章展示。`generate-config --check`、`tsc -b --noEmit`、`eslint .`、完整 `vitest`、`vite build`、`verify-layout` 和本地 Playwright desktop/mobile 技术词与横向溢出检查均已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-009`。
- DONE：审查后采集与数据关系修复已完成。RSS 采集增加明确 User-Agent、默认标签/公司关系和 `max_items` 限制；daily scheduled maintenance 自动执行实体/专题增量 upsert；实体回填不再清空全表；旧 schema 下实体匹配和翻译回填降级处理；前端公司/专题筛选改用 API 数据且配置类查询停止 5 分钟轮询。`generate-config --check`、`tsc -b --noEmit`、`eslint .`、完整 `vitest`、`vite build`、`verify-layout`、`git diff --check` 和运行时代码改动 secret-pattern scan 均已通过。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-008`。
- DONE：UI 主导航从五栏/六入口收敛为四个主入口，并将公司库、专题追踪降级为次级索引入口；`vitest` 局部测试、`tsc -b --noEmit`、`eslint .`、`vite build`、`verify-layout` 和本地 Playwright desktop/mobile 布局检查均已通过。
- DONE：UI 文案去“情报流/AI 说明味”已完成。主入口改为“资讯”，页面统一移除固定解释副标题，首页移除“聚合商业航天新闻...”说明，右侧“情报索引/配置热词”改为“来源状态”；本地 text/overflow 检查、`verify-layout`、完整测试和 build 均已通过。
- IN_PROGRESS：Launch Library 2 endpoint 已加入明确 User-Agent 且 scheduled Worker 已可部署；2026-05-30 公开 `/api/launches?limit=5` 已确认返回未来发射窗口，仍需具备 admin token 后复跑 production `/api/admin/ingest/launches` 才能关闭手动 refetch 验证。
- DONE：治理文档整理已完成。已拆分稳定约束、任务账本和问题账本，避免约束文件承担任务流水和 bug 账本职责；`tsc -b --noEmit`、`eslint .`、diff check 和 secret-pattern scan 均已通过。
- IN_PROGRESS：生产文章相关 API 500 的用户可见故障已复查通过。2026-05-30 生产 `/api/home?limit=12`、`/api/articles?limit=5`、`/api/articles/1`、`/api/companies/spacex` 和 `/api/topics/satellite-internet` 均返回 200，说明旧 schema 兼容兜底已解除公开页面 500；仍需具备 Cloudflare 权限后确认 D1 `0004_article_translation_fields` 迁移状态。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-005`。
- DONE：已修复政策页为空和采集入库兼容问题。政策页按 `policy-and-regulation` 标签展示官方页、采购页和 RSS 政策记录；采集入库在生产 D1 缺翻译字段时会降级写入旧 schema，避免爬虫抓到内容后整源失败。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-006` 和 `SI-ISSUE-007`。
- DONE：修复 scheduled ingestion 与 config-first/source-enabled 约束的偏差。`snapi` 和 `launch-library-2` 已尊重 `enabled`，新增 `procurement_page` 公开采购公告源类型和中国政府采购网中央公告源。跟踪项见 `SPACE_INTEL_ISSUES.md` 中 `SI-ISSUE-003` 和 `SI-ISSUE-004`。
- DONE：将“资本”完整替换为“政策”。已移除 market seed/API/schema 路径，新增 `/policy` 页面、政策动态 HUD、地方政府官方政策源、通用 HTML 列表轻爬虫解析能力和 `0005_drop_market_items.sql`；typecheck、lint、test、build、config check、layout 和本地浏览器检查均已通过。
- TODO：继续观察新增 RSS 和官方网页来源的采集质量、重复率和相关性；Google News RSS 仅作为必要时手动启用的备用聚合源。
- TODO：后续大版本依赖升级单独评估，不混入常规 bug 修复或来源维护任务。

最近完成：

- DONE：2026-05-28 data retention cleanup 已完成本地实现。新增 D1 retention cleanup 模块并接入 daily scheduled maintenance；默认保留 article metadata / article relation rows 730 天、ingestion logs 90 天、launch cache 730 天，按 bounded batch 删除；本地 typecheck、lint、test、build、layout、config parity、diff check 和 secret scan 均已通过，待 PR/CI。
- DONE：2026-05-28 Hy-MT 1.8B 翻译接入已合入 main。计划文件为 `docs/TRANSLATION_INTEGRATION_PLAN.md`；实现范围包括自部署 OpenAI-compatible 翻译服务 adapter、文章翻译字段、采集链路翻译 enrich、受保护历史回填 endpoint 和 API 翻译元数据返回。
- DONE：2026-05-28 architecture hardening 已完成并合入。包含 `pnpm generate:config` / `pnpm check:config`、配置漂移检查、scheduled ingestion 有界并发、每日 catalog sync、stale ingestion log maintenance、D1 查询索引、D1 batch 优先写入和 `/api/health` 增强。
- DONE：2026-05-28 production consistency 修复已完成并合入。同步 sources catalog 到 D1、让 `/api/launches` 默认只返回未来发射、清理历史未闭合 ingestion logs，并通过 `main` GitHub Actions verify 与 scheduled Worker deploy。
- DONE：Cloudflare 重建与线上采集问题修复已部署。scheduled ingestion 增加 25 秒单源超时并闭合失败日志；Launch Library 2 改为每 6 小时运行一次以降低 429；`/api/health` 返回最近已闭合日志和 `openIngestionLogCount`。
- DONE：第五轮回归审查已完成。Round 4 用户可见文案、真实标签/公司关系和来源配置修复有效；文章列表聚合分页 `hasMore` 误判、布局验证旧文案断言和 `.claude/` 本地配置忽略已修复。

## Milestones

| Status | Milestone | Acceptance |
| --- | --- | --- |
| DONE | Project Governance baseline | `AGENTS.md`, `SPACE_INTEL_DEV_CONSTRAINTS.md`, `SPACE_INTEL_TASKS.md` exist and define the original governance path. |
| DONE | Governance split | Stable constraints, task ledger, and issue ledger have separate responsibilities. |
| DONE | GitHub And Cloudflare Setup | Public repository, protected branches, Git-backed Pages, production domain, D1, R2, Pages secret, and scheduled Worker deploy path are configured. |
| DONE | Project Skeleton | React/Vite/TypeScript app, Tailwind, Wrangler configs, CI, and layout verification path are available. |
| DONE | Architecture Guardrails | Reference review, plugin-style source interface, optional VPS enrichment boundary, and current architecture notes are documented. |
| DONE | Data Foundation | D1 schema, source/company/topic/curation configs, generated config checks, and catalog sync are implemented. |
| DONE | Ingestion Pipeline | SNAPI, Launch Library 2, RSS, Google News RSS backup, official page ingestion, deduplication, ingestion logs, scheduled ingestion, and retention cleanup exist. |
| DONE | API Layer | Home, article, company, launch, topic, curation, enrichment, seed, and health endpoints exist with protected admin routes where needed. |
| DONE | Frontend Pages | Home, article list/detail, company, launch, policy, and topic pages are implemented with local fallback data where appropriate. |
| DONE | Curation And Editorial Controls | Manual curation loader, protected curation endpoint, ranking rules, and config-backed curated content exist. |
| DONE | Verification And Launch | CI, production Pages, preview deployment, D1/R2 binding checks, responsive layout checks, and existing-service safety checks have passed in prior release work. |

## Open Decisions

| Status | Decision | Default |
| --- | --- | --- |
| DONE | Final GitHub repository visibility | Repository is public as of 2026-05-09, which allowed branch protection on the free GitHub plan. |
| DONE | Final production subdomain | `space.bytebaud.com` is configured as the production Pages custom domain. |
| DONE | AI summary provider | Deferred decision recorded: Cloudflare AI Gateway, external LLM API, or isolated Hy-MT service after cost and quality review. |
| DONE | RSSHub hosting mode | Default decision recorded: public routes first; VPS Docker only when necessary and isolated. |
| DONE | Issue tracking location | Current bug/problem state is tracked in `SPACE_INTEL_ISSUES.md`; review reports are historical evidence. |

## Task Update Rules

- Update this file when starting, completing, blocking, or changing a major task.
- Keep entries short and current. Long investigation details belong in `docs/` or `SPACE_INTEL_ISSUES.md`.
- Do not duplicate full review reports here; link to the relevant report or issue ID.
- Record verification status honestly: passed, blocked, interrupted, timed out, suggested, or unverified.
- If a task discovers a bug, add or update an issue in `SPACE_INTEL_ISSUES.md` instead of burying it in task prose.
